import React,{useState}from'react';import{CalendarDays,Leaf}from'lucide-react';import{getCropRecommendations}from'./data.js';import RecommendationView from'./RecommendationView.jsx';import SchedulePlanner from'./SchedulePlanner.jsx';export default function PlannerScreen(props){const[view,setView]=useState('plant'),recommendations=getCropRecommendations(new Date());return <main className="plan-plant-shell"><section className="plan-plant-switch"><button className={view==='plant'?'active':''} onClick={()=>{setView('plant');requestAnimationFrame(()=>window.scrollTo(0,0))}}><Leaf/>What can we grow?</button><button className={view==='plan'?'active':''} onClick={()=>{setView('plan');requestAnimationFrame(()=>window.scrollTo(0,0))}}><CalendarDays/>Upcoming plan</button></section>{view==='plant'?<RecommendationView recommendations={recommendations} setModal={props.setModal}/>:<SchedulePlanner {...props}/>}</main>}