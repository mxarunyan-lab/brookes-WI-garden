import React,{useMemo,useState}from'react';import{Droplets,Leaf,Repeat2,Search,Sun,X}from'lucide-react';import{DetailModal as LegacyDetailModal}from'./screens.jsx';import SeasonalChore from'./SeasonalChore.jsx';import{cropCatalog,getCropProfile}from'./data.js';
const STAGES=['Seed','Germinating','Seedling','Growing','Flowering','Producing','Ready to harvest','Overwintering'],SUCCESSION={lettuce:14,spinach:14,radish:10,carrot:21,'green-bean':21,peas:21,basil:21,onion:21,kale:21};
function SoilCheck({modal,recordSoilCheck,recordWatered,close}){const[result,setResult]=useState(''),crop=modal.crop||getCropProfile(modal.plant.cropId),depth=crop?.checkDepth||'about 1 inch',water=crop?.watering||'Water slowly only if the root zone is becoming dry.';const choose=v=>{setResult(v);recordSoilCheck(modal.plant.id,v)};return <div><span className="modal-kicker">CROP-SPECIFIC MOISTURE CHECK</span><h2>Check {modal.plant.name}</h2><p><strong>Check {depth} down.</strong> {crop?.summary}</p><div className="soil-choice-grid">{[['dry',Sun,'Dry'],['damp',Leaf,'Damp'],['wet',Droplets,'Wet']].map(([v,I,l])=><button key={v} className={result===v?'selected':''} onClick={()=>choose(v)}><I/><strong>{l}</strong></button>)}</div>{result==='dry'&&<div className="soil-result result-dry"><strong>Watering is appropriate.</strong><span>{water}</span></div>}<div className="modal-action-row">{result==='dry'?<button className="primary-button" onClick={()=>{recordWatered(modal.plant.id);close()}}>I watered it</button>:<button className="primary-button" disabled={!result} onClick={close}>Save soil check</button>}</div></div>}
function AddPlant({modal,garden,addPlant,close}){const first=garden.spaces.find(s=>!s.hidden&&!s.deletedAt),seeds=(garden.seeds||[]).filter(s=>!s.deletedAt);const[mode,setMode]=useState(modal.prefill?'details':seeds.length?'seeds':'catalog'),[search,setSearch]=useState('');const initial=modal.prefill||{};const[d,setD]=useState({name:initial.name||'',variety:initial.variety||'',cropId:initial.id||'',seedId:initial.seedId||'',source:initial.source||'manual',spaceId:modal.spaceId||first?.id||'',stage:initial.stage||'Growing',plantedAt:new Date().toISOString().slice(0,10),growingGoal:'seasonal',containerSize:'',growLightId:'',lightHours:16,notes:initial.summary||'',quantity:1,successionEnabled:false,successionDays:SUCCESSION[initial.id]||14});const matches=useMemo(()=>cropCatalog.filter(c=>`${c.name} ${c.variety} ${c.family}`.toLowerCase().includes(search.toLowerCase())),[search]),crop=getCropProfile(d.cropId),suggested=SUCCESSION[d.cropId],space=garden.spaces.find(s=>s.id===d.spaceId),indoor=['indoor','basement','greenhouse','hydro'].includes(space?.type);const choose=(name,variety,cropId,seedId='',source='catalog',notes='')=>{setD(v=>({...v,name,variety,cropId,seedId,source,notes:v.notes||notes,successionDays:SUCCESSION[cropId]||14}));setMode('details')};const submit=e=>{e.preventDefault();if(d.name&&d.spaceId){addPlant(d);close()}};return <div className="add-plant-flow"><span className="modal-kicker">ADD WHAT IS ACTUALLY GROWING</span><h2>Add a plant</h2><div className="source-tabs"><button className={mode==='seeds'?'active':''} onClick={()=>setMode('seeds')}>My seeds</button><button className={mode==='catalog'?'active':''} onClick={()=>setMode('catalog')}>Crop list</button><button className={mode==='manual'?'active':''} onClick={()=>setMode('manual')}>Enter manually</button></div>{mode==='seeds'&&<div className="catalog-picker">{seeds.map(s=><button key={s.id} onClick={()=>{const c=getCropProfile(s.cropId);choose(s.name,s.variety||c?.variety||'',s.cropId||c?.id||'',s.id,'seed',c?.summary)}}><strong>{s.name}</strong><small>{s.variety||'Variety not entered'} • packet {s.packetYear}</small></button>)}</div>}{mode==='catalog'&&<div className="catalog-picker"><label className="catalog-search"><Search/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search crops…"/></label>{matches.map(c=><button key={c.id} onClick={()=>choose(c.name,c.variety,c.id,'','catalog',c.summary)}><strong>{c.name}</strong><small>{c.family} • {c.harvest}</small></button>)}</div>}{(mode==='details'||mode==='manual')&&<form className="modal-form" onSubmit={submit}><label>Plant or crop name<input value={d.name} onChange={e=>setD({...d,name:e.target.value})} required/></label><label>Variety<input value={d.variety} onChange={e=>setD({...d,variety:e.target.value})}/></label><label>Plant location<select value={d.spaceId} onChange={e=>setD({...d,spaceId:e.target.value})}>{garden.spaces.filter(s=>!s.hidden&&!s.deletedAt).map(s=><option key={s.id} value={s.id}>{s.name}</option>)}</select><small className="field-help">Choose the exact bed, container, greenhouse, or indoor area where this plant is growing.</small></label><label>Current growth stage<select value={d.stage} onChange={e=>setD({...d,stage:e.target.value})}>{STAGES.map(x=><option key={x}>{x}</option>)}</select></label><label>Date started or planted<input type="date" value={d.plantedAt} onChange={e=>setD({...d,plantedAt:e.target.value})}/></label>{suggested&&<section className="succession-suggestion"><Repeat2/><div><strong>Succession planting can help with {crop?.name||d.name}</strong><p>That means planting a smaller new batch every few weeks instead of harvesting everything at once. The Planting Desk will use your saved seed packet and remind you when the next batch is due.</p><label><input type="checkbox" checked={d.successionEnabled} onChange={e=>setD({...d,successionEnabled:e.target.checked})}/> Plan repeat batches</label>{d.successionEnabled&&<label>Days between batches<input type="number" min="7" max="60" value={d.successionDays} onChange={e=>setD({...d,successionDays:Number(e.target.value)})}/></label>}</div></section>}{(indoor||d.growingGoal!=='seasonal')&&<div className="indoor-goal-fields"><label>Container size<input value={d.containerSize} onChange={e=>setD({...d,containerSize:e.target.value})}/></label><label>Light hours<input type="number" value={d.lightHours} onChange={e=>setD({...d,lightHours:e.target.value})}/></label></div>}<label>Notes<textarea value={d.notes} onChange={e=>setD({...d,notes:e.target.value})}/></label><button className="primary-button">Add plant to {space?.name||'selected location'}</button></form>}</div>}
function AddSpace({addSpace,close}){const[d,setD]=useState({name:'',type:'black-square-bed',capacity:12});return <form className="modal-form" onSubmit={e=>{e.preventDefault();addSpace(d);close()}}><span className="modal-kicker">ADD A BED, CONTAINER, OR GROWING AREA</span><h2>Add a garden space</h2><label>Garden space name<input value={d.name} onChange={e=>setD({...d,name:e.target.value})} placeholder="Example: White Oval Bed #1" required/><small className="field-help">Give this specific bed, container, or area a name you will recognize later.</small></label><label>Growing area or container type<select value={d.type} onChange={e=>setD({...d,type:e.target.value})}><option value="black-square-bed">Black square raised bed</option><option value="white-oval-bed">White rounded raised bed</option><option value="bed">Other raised bed</option><option value="in-ground">In-ground garden bed</option><option value="container">Container or pot</option><option value="greenhouse">Greenhouse</option><option value="indoor">Indoor growing area</option><option value="basement">Basement growing area</option><option value="hydro">Hydroponics</option></select><small className="field-help">This describes what the space physically is. The name above identifies the exact one.</small></label><label>Approximate plant capacity<input type="number" min="1" max="200" value={d.capacity} onChange={e=>setD({...d,capacity:Number(e.target.value)})}/></label><button className="primary-button">Add garden space</button></form>}
export default function ClearDetailModal(props){const{modal,close}=props;if(modal.type==='seasonalChore')return <div className="overlay modal-overlay" onMouseDown={close}><section className="detail-modal seasonal-detail-modal" onMouseDown={e=>e.stopPropagation()}><SeasonalChore task={modal.task} garden={props.garden} onAdd={crop=>props.setModal({type:'addPlant',prefill:crop})} onDone={props.completeTask} onSnooze={props.snoozeTask} onSkip={props.skipTask} close={close}/></section></div>;if(!['addPlant','addSpace','soilCheck'].includes(modal.type))return <LegacyDetailModal {...props}/>;return <div className="overlay modal-overlay" onMouseDown={close}><section className="detail-modal" onMouseDown={e=>e.stopPropagation()}><button className="modal-close" onClick={close}><X/></button>{modal.type==='addPlant'?<AddPlant {...props}/>:modal.type==='addSpace'?<AddSpace {...props}/>:<SoilCheck {...props}/>}</section></div>}