import React,{useState}from'react';
import{CalendarDays,Leaf}from'lucide-react';
import PlantRecommendations from'./PlantRecommendations.jsx';
import PlannerScreen from'./PlannerScreen.jsx';
export default function PlanPlantHub(props){const[view,setView]=useState('grow');return <main className="screen plan-plant-page"><section className="dark-header plan-plant-header"><span>WHAT SHOULD WE DO NEXT?</span><h1>Plan & Plant</h1><p>Seasonal planting guidance and the upcoming Runyan Garden schedule in one clean place.</p></section><section className="plan-plant-body screen-pad"><div className="plan-plant-switch" role="tablist"><button role="tab" aria-selected={view==='grow'} className={view==='grow'?'active':''} onClick={()=>setView('grow')}><Leaf/> What can we grow?</button><button role="tab" aria-selected={view==='plan'} className={view==='plan'?'active':''} onClick={()=>setView('plan')}><CalendarDays/> Upcoming plan</button></div><div className="plan-plant-view" key={view}>{view==='grow'?<PlantRecommendations embedded recommendations={props.recommendations} setModal={props.setModal}/>:<PlannerScreen embedded garden={props.garden} timeline={props.timeline} addSeed={props.addSeed} completePlanItem={props.completePlanItem} updatePlantStage={props.updatePlantStage} recordHarvest={props.recordHarvest} scheduleSuccession={props.scheduleSuccession}/>}</div></section></main>}