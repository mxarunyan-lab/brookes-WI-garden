import React from'react';import{Bell,Check,ChevronDown,ChevronRight,Cloud,CloudRain,CloudSun,Droplets,Leaf,MapPin,Menu,Moon,RefreshCw,Snowflake,Sun}from'lucide-react';import{formatDateTime}from'./data.js';import{WisconsinLandscape}from'./art.jsx';
function WeatherIcon({weather,size=38}){const c=weather?.weatherCode??2;if([71,73,75,77,85,86].includes(c))return <Snowflake size={size}/>;if([51,53,55,61,63,65,66,67,80,81,82,95,96,99].includes(c))return <CloudRain size={size}/>;if([3,45,48].includes(c))return <Cloud size={size}/>;if(!weather?.isDay)return <Moon size={size}/>;if(c===0)return <Sun size={size}/>;return <CloudSun size={size}/>}
function WeatherLine({weather,loading,error,refresh,alert}){const calm=!alert||alert.type==='good';return <section className="compact-weather-line"><div className="compact-weather-reading"><WeatherIcon weather={weather}/><span><strong>{weather?`${weather.temperature}° • ${weather.condition}`:'Green Bay weather'}</strong><small>{loading?'Updating weather…':error||`High ${weather?.high??'—'}° • ${weather?.rainChance??'—'}% rain`}</small></span></div><div className={`compact-weather-message ${calm?'is-calm':''}`}><Check/><span><strong>{calm?'No major weather concerns today':alert.title}</strong><small>{calm?'Normal garden care should be fine.':alert.detail}</small></span></div><button onClick={refresh}><RefreshCw/></button></section>}
export function TodayWorkspace({profile,garden,tasks,dailyDone,onTask,onMenu,noticeOpen,setNoticeOpen,weatherState,weatherAlert,activity,seasonMode,navigate,tomorrowTasks=[]}){const completeIds=new Set((dailyDone||[]).map(x=>typeof x==='string'?x:x.id)),active=tasks.filter(t=>!completeIds.has(t.id)),outdoor=active.filter(t=>!['greenhouse'].includes(t.kind)&&t.target!=='indoor').length,indoor=active.filter(t=>t.kind==='greenhouse'||t.target==='indoor').length,planting=active.filter(t=>t.kind==='navigate').length,last=(activity||[]).slice(0,2);return <main className="screen today-screen"><section className="today-hero"><button className="round-control hero-menu" onClick={onMenu}><Menu/></button><button className="round-control hero-bell" onClick={()=>setNoticeOpen(v=>!v)}><Bell/>{active.length>0&&<span className="notification-count">{active.length}</span>}</button>{noticeOpen&&<div className="notification-popover actionable-briefing"><strong>Garden briefing</strong><span>{active.length} unfinished action{active.length===1?'':'s'} today.</span><button onClick={()=>navigate('chores')}>Open Chore Board <ChevronRight/></button></div>}<div className="hero-title-wrap"><Leaf/><h1>{profile.gardenerName||'Brooke'}’s<br/>Garden</h1><p>The Runyan Garden • Green Bay</p></div><WisconsinLandscape/></section><section className="today-body compact-today-body screen-pad"><WeatherLine {...weatherState} alert={weatherAlert}/><section className="bulletin-card"><span className="section-kicker">THE GARDEN BULLETIN</span><h2>{active.length?`${active.length} thing${active.length===1?'':'s'} need attention`:'Nothing urgent today'}</h2><p>{active.length?`${outdoor} outdoor • ${indoor} indoor • ${planting} planting`:'The app will put work back on the board when it matters.'}</p><button className="primary-button" onClick={()=>navigate('chores')}>Open Garden Chore Board <ChevronRight/></button><details className="tomorrow-preview"><summary><span><strong>Tomorrow preview</strong><small>{tomorrowTasks.length} expected task{tomorrowTasks.length===1?'':'s'}</small></span><ChevronDown/></summary><div>{tomorrowTasks.slice(0,4).map(t=><button key={t.id} onClick={()=>navigate('chores')}><span>{t.title}<small>{t.subtitle}</small></span><ChevronRight/></button>)}{!tomorrowTasks.length&&<p>Nothing is currently expected tomorrow.</p>}</div></details></section><section className="compact-handoff"><div><span className="section-kicker">LATEST HANDOFF</span><h2>What already happened</h2></div>{last.length?last.map(entry=><article key={entry.id}><Check/><span><strong>{entry.actor?`${entry.actor}: `:''}{entry.title}</strong><small>{entry.detail}</small></span><em>{formatDateTime(entry.at)}</em></article>):<p>No garden activity recorded yet.</p>}</section><div className="season-note-compact"><strong>{seasonMode?.headline}</strong><span>{seasonMode?.focus}</span></div></section></main>}
