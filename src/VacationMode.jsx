import React,{useMemo,useState}from'react';
import{AlertTriangle,CalendarRange,Check,ChevronDown,CloudSun,Edit3,Printer,RefreshCw,Sprout,Trash2,UserRound,X}from'lucide-react';
import{SecondaryHero,SecondarySectionHeader}from'./SecondaryUI.jsx';
import{formatGardenDate}from'./dateFormat.js';
import{buildVacationPlan,vacationPlanNeedsReview}from'./vacationPlanner.js';

const today=()=>new Date().toISOString().slice(0,10);
const plusDays=days=>{const d=new Date();d.setDate(d.getDate()+days);return d.toISOString().slice(0,10)};
const sectionInfo={before:{title:'Before leaving',description:'Work that reduces avoidable risk before the trip.'},during:{title:'During the trip',description:'Specific caretaker checks and weather exceptions.'},after:{title:'After returning',description:'Inspection and history reconciliation.'}};

function TripSetup({garden,weather,onSave,existing=null,onCancel}){const[d,setD]=useState(()=>({departureDate:existing?.departureDate||plusDays(3),returnDate:existing?.returnDate||plusDays(8),caretakerAvailable:existing?.caretakerAvailable??true,caretakerName:existing?.caretakerName||'',caretakerNotes:existing?.caretakerNotes||''})),[error,setError]=useState('');const submit=e=>{e.preventDefault();if(!d.departureDate||!d.returnDate||d.returnDate<d.departureDate){setError('Return date must be on or after the departure date.');return}const plan=buildVacationPlan({garden,weather,actor:garden.profile?.gardenerName||'Brooke',id:existing?.id,...d});onSave(plan)};return <form className="vacation-setup-card" onSubmit={submit}><div className="vacation-form-heading"><span><small>TRIP DETAILS</small><strong>{existing?'Update trip dates':'Create a connected vacation plan'}</strong></span>{onCancel&&<button type="button" onClick={onCancel} aria-label="Close trip editor"><X/></button>}</div><div className="vacation-two-fields"><label>Departure date<input type="date" min={today()} value={d.departureDate} onChange={e=>setD({...d,departureDate:e.target.value})} required/></label><label>Return date<input type="date" min={d.departureDate||today()} value={d.returnDate} onChange={e=>setD({...d,returnDate:e.target.value})} required/></label></div><label className="vacation-checkbox"><input type="checkbox" checked={d.caretakerAvailable} onChange={e=>setD({...d,caretakerAvailable:e.target.checked})}/> A caretaker can check the garden</label>{d.caretakerAvailable&&<label>Caretaker name<input value={d.caretakerName} onChange={e=>setD({...d,caretakerName:e.target.value})} placeholder="Optional name"/></label>}<label>Handoff note<textarea rows="3" value={d.caretakerNotes} onChange={e=>setD({...d,caretakerNotes:e.target.value})} placeholder="Gate access, hose location, plants not to touch, or emergency contact details"/></label>{error&&<p className="vacation-error" role="alert">{error}</p>}<button className="primary-button" type="submit"><CalendarRange/>{existing?'Rebuild around new dates':'Build trip plan'}</button></form>}

function InstructionEditor({task,onSave,onCancel}){const[d,setD]=useState({...task});return <form className="vacation-task-editor" onSubmit={e=>{e.preventDefault();onSave({...d,manualEdited:true,updatedAt:new Date().toISOString()})}}><label>Instruction<textarea rows="3" value={d.instruction} onChange={e=>setD({...d,instruction:e.target.value})}/></label><label>What to check<textarea rows="2" value={d.whatToCheck||''} onChange={e=>setD({...d,whatToCheck:e.target.value})}/></label><label>What not to do<textarea rows="2" value={d.whatNotToDo||''} onChange={e=>setD({...d,whatNotToDo:e.target.value})}/></label><label>Weather exception<textarea rows="2" value={d.weatherException||''} onChange={e=>setD({...d,weatherException:e.target.value})}/></label><div><button className="primary-button" type="submit"><Check/>Save instruction</button><button type="button" onClick={onCancel}>Cancel</button></div></form>}

function VacationTask({task,onComplete,onUpdate}){const[editing,setEditing]=useState(false);if(editing)return <InstructionEditor task={task} onSave={next=>{onUpdate(next);setEditing(false)}} onCancel={()=>setEditing(false)}/>;return <article className={`vacation-task-card priority-${String(task.urgency||'normal').toLowerCase()}`}><button className={`vacation-task-check ${task.status==='completed'?'is-complete':''}`} onClick={()=>onComplete(task)} aria-label={`${task.status==='completed'?'Undo':'Complete'} ${task.action}`}><Check/></button><div><div className="vacation-task-title"><span><small>{task.urgency||'Normal'} · {formatGardenDate(task.dueDate)}</small><strong>{task.action}</strong></span>{task.manualEdited&&<em>MANUALLY EDITED</em>}</div><h3>{task.targetName}</h3><p>{task.instruction}</p>{Boolean(task.whatToCheck||task.whatNotToDo||task.weatherException)&&<details className="vacation-task-instructions"><summary><span>Check details</span><ChevronDown/></summary><div>{task.whatToCheck&&<small><b>Check first:</b> {task.whatToCheck}</small>}{task.whatNotToDo&&<small><b>Do not:</b> {task.whatNotToDo}</small>}{task.weatherException&&<small><b>Weather exception:</b> {task.weatherException}</small>}</div></details>}</div><button className="vacation-edit" onClick={()=>setEditing(true)} aria-label={`Edit ${task.action}`}><Edit3/></button></article>}

function printCaretaker(plan){const popup=window.open('','_blank','width=760,height=900');if(!popup)return window.alert('Allow pop-ups to print the caretaker handoff.');const tasks=(plan.tasks||[]).filter(task=>task.section==='during'&&task.status!=='completed'),escape=value=>String(value||'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));popup.document.write(`<!doctype html><html><head><title>Runyan Garden Vacation Handoff</title><style>body{font-family:Arial,sans-serif;color:#173d2b;margin:36px}h1{font-family:Georgia,serif}article{border:1px solid #9aa79e;border-radius:12px;padding:14px;margin:12px 0;page-break-inside:avoid}small{display:block;margin:5px 0;color:#40594b}.box{font-size:22px;margin-right:8px}</style></head><body><h1>Runyan Garden Vacation Handoff</h1><p>${escape(formatGardenDate(plan.departureDate))} through ${escape(formatGardenDate(plan.returnDate))}${plan.caretakerName?` · For ${escape(plan.caretakerName)}`:''}</p>${plan.caretakerNotes?`<p><strong>Household note:</strong> ${escape(plan.caretakerNotes)}</p>`:''}${tasks.map(task=>`<article><h2><span class="box">☐</span>${escape(task.targetName)}</h2><p><strong>${escape(formatGardenDate(task.dueDate))}:</strong> ${escape(task.instruction)}</p>${task.whatToCheck?`<small><b>Check first:</b> ${escape(task.whatToCheck)}</small>`:''}${task.whatNotToDo?`<small><b>Do not:</b> ${escape(task.whatNotToDo)}</small>`:''}${task.weatherException?`<small><b>Weather exception:</b> ${escape(task.weatherException)}</small>`:''}<p>Notes: ______________________________________________</p></article>`).join('')||'<p>No caretaker tasks are required for this plan.</p>'}<script>window.onload=()=>window.print()</script></body></html>`);popup.document.close()}

export default function VacationMode({garden,weather,navigate,onSavePlan,onRefreshPlan,onUpdateTask,onCompleteTask,onClosePlan}){const active=useMemo(()=>(garden.vacationPlans||[]).filter(plan=>!plan.deletedAt&&plan.status==='active').sort((a,b)=>String(b.updatedAt).localeCompare(String(a.updatedAt)))[0],[garden.vacationPlans]),[editing,setEditing]=useState(false),needsReview=active&&vacationPlanNeedsReview(active,weather);const groups=active?['before','during','after'].map(section=>({section,tasks:(active.tasks||[]).filter(task=>task.section===section&&!task.deletedAt)})):[];return <main className="screen secondary-screen vacation-mode-screen"><SecondaryHero icon={CalendarRange} eyebrow="CONNECTED TRIP CARE" title="Vacation Mode" description="A garden-specific before, during, and after plan built from actual plants, spaces, stages, care history, and weather." onBack={()=>navigate('center')} backLabel="Back to Garden Center" className="center-department-hero vacation-mode-hero"/><section className="screen-pad secondary-screen-content vacation-mode-content">{!active||editing?<TripSetup garden={garden} weather={weather} existing={editing?active:null} onSave={plan=>{onSavePlan(plan);setEditing(false)}} onCancel={editing?()=>setEditing(false):null}/>:<><section className="vacation-plan-summary"><div><small>ACTIVE TRIP PLAN</small><h2>{formatGardenDate(active.departureDate)}–{formatGardenDate(active.returnDate)}</h2><p>{active.duration} days · {active.caretakerAvailable?`${active.caretakerName||'Caretaker'} handoff included`:'No caretaker available'}</p></div><span><CloudSun/><strong>{active.weatherSummary?.high!==null?`${Math.round(active.weatherSummary.high)}° expected high`:'Forecast incomplete'}</strong><small>{active.weatherSummary?.meaningfulRain?'Meaningful rain is possible; it is not counted until observed.':'No dependable rain credit is assumed.'}</small></span></section>{needsReview&&<section className="vacation-change-alert"><AlertTriangle/><span><strong>The forecast changed meaningfully.</strong><small>Re-evaluate only the affected instructions. Manually edited caretaker text will be preserved.</small></span><button onClick={()=>onRefreshPlan(active)}><RefreshCw/>Review changes</button></section>}<div className="vacation-plan-actions"><button onClick={()=>setEditing(true)}><Edit3/>Change trip details</button><button onClick={()=>printCaretaker(active)}><Printer/>Print caretaker handoff</button><button className="danger-control" onClick={()=>onClosePlan(active.id)}><Trash2/>End trip plan</button></div>{active.changes?.length>0&&<details className="vacation-change-list"><summary><span><strong>What changed</strong><small>{active.changes.length} instruction change{active.changes.length===1?'':'s'} after re-evaluation</small></span><ChevronDown/></summary>{active.changes.map((change,index)=><article key={`${change.taskId}-${index}`}><strong>{change.title}</strong><p>{change.reason}</p><small>Previous: {change.from}</small><small>Now: {change.to}</small></article>)}</details>}{groups.map(group=><section className="vacation-task-section" key={group.section}><SecondarySectionHeader eyebrow={group.section.toUpperCase()} title={sectionInfo[group.section].title} description={sectionInfo[group.section].description} count={group.tasks.length}/>{group.tasks.length?<div className="vacation-task-list">{group.tasks.map(task=><VacationTask key={task.id} task={task} onComplete={onCompleteTask} onUpdate={next=>onUpdateTask(active.id,next)}/>)}</div>:<p className="vacation-empty">No work is currently required in this section.</p>}</section>)}<section className="vacation-honesty"><UserRound/><span><strong>This plan uses saved garden records.</strong><small>Weather exceptions remain forecasts until observed. Caretaker edits are preserved, and completed trip work is retained in history rather than regenerated.</small></span></section></>}</section></main>}
