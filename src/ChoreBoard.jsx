import React,{useEffect,useMemo,useState}from'react';
import{CalendarPlus,Check,ChevronDown,ClipboardList,CloudRain,Leaf,Plus,Sprout,Undo2,Warehouse,XCircle}from'lucide-react';
import{formatDateTime}from'./data.js';
import{SecondaryHero}from'./SecondaryUI.jsx';
import{buildTaskBoard,completionFor,filterEmptyMessage,taskArea}from'./taskBoard.js';
const todayKey=()=>new Date().toISOString().slice(0,10);
const dueLabel=task=>{if(!task.dueDate||task.dueDate===todayKey())return'Due today';if(task.dueDate<todayKey())return`Overdue since ${task.dueDate}`;return`Due ${task.dueDate}`};
const taskName=task=>task.plant?.name||task.space?.name||task.title||'';
const sortTasks=tasks=>[...tasks].sort((a,b)=>{const urgent=Number(b.priority>=90||b.priorityLabel==='Urgent')-Number(a.priority>=90||a.priorityLabel==='Urgent');if(urgent)return urgent;const overdue=Number(Boolean(b.dueDate&&b.dueDate<todayKey()))-Number(Boolean(a.dueDate&&a.dueDate<todayKey()));if(overdue)return overdue;const date=String(a.dueDate||todayKey()).localeCompare(String(b.dueDate||todayKey()));if(date)return date;const time=String(a.dueTime||a.when||'').localeCompare(String(b.dueTime||b.when||''));if(time)return time;const priority=Number(b.priority||0)-Number(a.priority||0);if(priority)return priority;return taskName(a).localeCompare(taskName(b))});
function TaskIcon({task,done}){if(done)return <Check/>;if(task.weatherDriven)return <CloudRain/>;if(taskArea(task)==='Greenhouse')return <Warehouse/>;if(task.kind==='navigate'||task.kind==='seasonalGuide')return <Leaf/>;return <Sprout/>}
function primaryAction(task){if(task.weatherDriven)return{label:'Review Alert',mode:'open'};if(task.taskType==='Check Moisture')return{label:'Check Moisture',mode:'open'};if(task.taskType==='Water')return{label:'Check & Record Watering',mode:'open'};if(task.taskType==='Harvest')return{label:'Log Harvest',mode:'open'};if(['Harden Off','Start Seeds','Monitor Germination','Check Emergence','Rotate Seedlings','Check Pollination'].includes(task.taskType))return{label:task.taskType==='Harden Off'?'Open Guided Steps':'Open Plant Journey',mode:'open'};const labels={'Fertilize':'Fertilizing Complete','Prune':'Pruning Complete','Stake':'Support Installed','Hill Potatoes':'Hilling Complete','Deadhead':'Deadheading Complete','Inspect For Pests':'Inspection Complete','Prepare Space':'Space Prepared','Transplant':'Mark as Transplanted'};return{label:labels[task.taskType]||'Done',mode:'complete'}}
function Row({task,completion,onOpen,onOpenPlant,onOpenSpace,onStatus,onUndo}){const[expanded,setExpanded]=useState(false),done=Boolean(completion),status=completion?.status||'',space=task.space?.name||task.plant?.spaceName||'',plant=task.plant?.name||'',important=Number(task.priority||0)>=70?task.priorityLabel:task.taskType||'Garden task',primary=primaryAction(task),dismissLabel=task.weatherDriven?'Dismiss':'Not needed',runPrimary=()=>primary.mode==='open'?onOpen(task):onStatus(task,'done');return <article className={`chore-row intelligence-task-row compact-task-card ${expanded?'is-expanded':''} ${done?'is-complete':''} ${status==='skipped'?'is-skipped':''} ${status==='not-needed'?'is-not-needed':''}`}><button className="task-card-summary" onClick={()=>setExpanded(value=>!value)} aria-expanded={expanded}><span className="chore-row-icon"><TaskIcon task={task} done={done}/></span><span className="task-card-summary-copy"><strong>{task.title}</strong><small>{done?(status==='skipped'?'Skipped':status==='not-needed'?'Not needed':'Completed'):`${important} · ${dueLabel(task)}`}</small>{(plant||space)&&<em>{[plant,space].filter(Boolean).join(' · ')}</em>}</span><ChevronDown className="task-card-chevron"/></button>{expanded&&<div className="task-card-expanded">{!done&&<><div className="task-meta-line"><span className={`task-priority priority-${String(task.priorityLabel||'Normal').toLowerCase()}`}>{task.priorityLabel||'Normal'}</span><span>{task.taskType||'Garden task'}</span><span>{dueLabel(task)}</span></div>{task.subtitle&&<p className="task-full-instructions">{task.subtitle}</p>}{task.reason&&<small><strong>Why:</strong> {task.reason}</small>}{task.when&&<small><strong>When:</strong> {task.when}</small>}</>}{done&&<p className="task-completion-copy">{status==='skipped'?'Skipped':status==='not-needed'?'Marked not needed':'Completed'} by {completion.actor} · {formatDateTime(completion.at)}</p>}<div className="task-expanded-actions">{done?<button className="task-undo-action" onClick={()=>onUndo(task.id)}><Undo2/>Undo</button>:<><button className="task-primary-outcome" onClick={runPrimary}>{primary.mode==='complete'&&<Check/>}{primary.label}</button>{(task.plant||task.space?.id)&&<div className="task-navigation-actions">{task.plant&&<button onClick={()=>onOpenPlant(task.plant)}>View Plant</button>}{task.space?.id&&<button onClick={()=>onOpenSpace(task.space.id)}>View Growing Space</button>}</div>}<div className="task-secondary-outcomes"><button onClick={()=>onStatus(task,'not-needed')}><XCircle/>{dismissLabel}</button>{!task.weatherDriven&&<button onClick={()=>onStatus(task,'skipped')}>Skip</button>}</div></>}</div></div>}</article>}
function TaskSection({id,kicker,title,tasks,completion,defaultOpen=false,emptyText='',initialLimit=8,...props}){const[open,setOpen]=useState(defaultOpen),[showAll,setShowAll]=useState(false),[resetKey,setResetKey]=useState(0);useEffect(()=>{if(defaultOpen)setOpen(true)},[defaultOpen]);if(!tasks.length)return emptyText?<section className="chore-section compact-empty-section"><div><span className="section-kicker">{kicker}</span><p>{emptyText}</p></div></section>:null;const ordered=sortTasks(tasks),limited=Number.isFinite(initialLimit)&&ordered.length>initialLimit,visible=showAll||!limited?ordered:ordered.slice(0,initialLimit);return <section className={`chore-section collapsible-chore-section ${open?'is-open':''}`}><button className="chore-section-toggle" onClick={()=>setOpen(value=>!value)} aria-expanded={open} aria-controls={`section-${id}`}><span><small>{kicker}</small><strong>{title}</strong></span><em>{tasks.length}</em><ChevronDown/></button>{open&&<div id={`section-${id}`} className="chore-section-body"><div className="task-section-controls"><small>{limited&&!showAll?`Showing ${visible.length} of ${ordered.length}`:`${ordered.length} task${ordered.length===1?'':'s'} · tap a title for details`}</small><button onClick={()=>setResetKey(value=>value+1)}>Collapse all cards</button></div><div className="chore-list">{visible.map(task=><Row key={`${task.id}-${resetKey}`} task={task} completion={completion(task.id)} {...props}/>)}</div>{limited&&<button className="show-all-tasks" onClick={()=>setShowAll(value=>!value)}>{showAll?`Show first ${initialLimit}`:`Show all ${ordered.length}`}</button>}</div>}</section>}
function ReminderForm({garden,onAdd,onClose}){const[d,setD]=useState({title:'',taskType:'Check Moisture',plantId:'',spaceId:'',priority:60,dueDate:todayKey(),reason:'',note:''}),selectedPlant=garden.plants.find(plant=>plant.id===d.plantId),submit=event=>{event.preventDefault();if(!d.title.trim())return;onAdd({...d,spaceId:d.spaceId||selectedPlant?.spaceId||''});onClose()};return <form className="manual-reminder-form" onSubmit={submit}><div className="manual-reminder-heading"><div><span className="section-kicker">MANUAL REMINDER</span><h2>Add a garden task</h2></div><button type="button" onClick={onClose} aria-label="Close reminder form"><XCircle/></button></div><label>Task name<input value={d.title} onChange={event=>setD({...d,title:event.target.value})} required/></label><label>Task type<select value={d.taskType} onChange={event=>setD({...d,taskType:event.target.value})}>{['Water','Check Moisture','Fertilize','Prune','Harvest','Stake','Hill Potatoes','Harden Off','Deadhead','Inspect For Pests'].map(type=><option key={type}>{type}</option>)}</select></label><div className="two-field-row"><label>Plant<select value={d.plantId} onChange={event=>setD({...d,plantId:event.target.value})}><option value="">No specific plant</option>{garden.plants.filter(plant=>!plant.deletedAt&&!plant.archived).map(plant=><option key={plant.id} value={plant.id}>{plant.name}</option>)}</select></label><label>Growing space<select value={d.spaceId} onChange={event=>setD({...d,spaceId:event.target.value})}><option value="">Use plant location</option>{garden.spaces.filter(space=>!space.deletedAt).map(space=><option key={space.id} value={space.id}>{space.name}</option>)}</select></label></div><div className="two-field-row"><label>Priority<select value={d.priority} onChange={event=>setD({...d,priority:Number(event.target.value)})}><option value="95">Urgent</option><option value="80">High</option><option value="60">Normal</option><option value="35">Low</option></select></label><label>Due date<input type="date" value={d.dueDate} onChange={event=>setD({...d,dueDate:event.target.value})}/></label></div><label>Reason<input value={d.reason} onChange={event=>setD({...d,reason:event.target.value})}/></label><label>Notes<textarea value={d.note} onChange={event=>setD({...d,note:event.target.value})}/></label><button className="primary-button"><CalendarPlus/>Add to Chore Board</button></form>}
export default function ChoreBoard({todayTasks,dailyDone,onOpen,onOpenPlant,onOpenSpace,onStatus,onUndo,navigate,garden,onAddReminder}){const[filter,setFilter]=useState('All'),[adding,setAdding]=useState(false),board=useMemo(()=>buildTaskBoard({tasks:todayTasks,completions:dailyDone,filter}),[todayTasks,dailyDone,filter]),urgentIds=new Set(board.sections.urgent.map(task=>task.id)),overdue=board.sections.overdue.filter(task=>!urgentIds.has(task.id)),overdueIds=new Set(overdue.map(task=>task.id)),weather=board.sections.weather.filter(task=>!urgentIds.has(task.id)&&!overdueIds.has(task.id)),weatherIds=new Set(weather.map(task=>task.id)),dueToday=board.sections.dueToday.filter(task=>!urgentIds.has(task.id)&&!weatherIds.has(task.id)),completion=id=>completionFor(dailyDone,id),filters=['All','Outdoor','Indoor',...(todayTasks.some(task=>taskArea(task)==='Greenhouse')?['Greenhouse']:[])],rowProps={completion,onOpen,onOpenPlant,onOpenSpace,onStatus,onUndo};return <main className="screen secondary-screen chore-board-screen"><SecondaryHero icon={ClipboardList} eyebrow="THE GARDEN BULLETIN" title="Garden Chore Board" description="See counts and urgency first. Open only the section and task you need." onBack={()=>navigate('center')} backLabel="Back to Garden Center" className="center-department-hero chore-board-hero"/><section className="screen-pad secondary-screen-content chore-board-content"><div className="chore-summary reconciled-summary"><div><strong>{board.counts.needsAttention} need attention</strong><small>Unique unfinished chores due today or overdue</small></div><div className="reconciled-counts"><span><b>{board.counts.urgent}</b> urgent</span><span><b>{board.counts.dueToday}</b> due today</span><span><b>{board.counts.overdue}</b> overdue</span><span><b>{board.counts.weather}</b> weather alerts</span><span><b>{board.counts.upcoming}</b> upcoming</span></div><button onClick={()=>setAdding(true)}><Plus/>Add reminder</button></div>{adding&&<ReminderForm garden={garden} onAdd={onAddReminder} onClose={()=>setAdding(false)}/>}<div className="chore-filters secondary-segmented-control" aria-label="Filter chores by growing area">{filters.map(item=><button key={item} className={filter===item?'active':''} aria-pressed={filter===item} onClick={()=>setFilter(item)}>{item}</button>)}</div>{!board.needsAttention.length&&!board.sections.upcoming.length&&<div className="all-clear"><Check/><span><strong>{filterEmptyMessage(filter)}</strong><small>Completed, skipped, and not-needed decisions remain saved below.</small></span></div>}<TaskSection id="urgent" kicker="URGENT" title="Needs attention first" tasks={board.sections.urgent} defaultOpen initialLimit={Infinity} {...rowProps}/><TaskSection id="overdue" kicker="OVERDUE" title="Past the due date" tasks={overdue} defaultOpen {...rowProps}/><TaskSection id="weather" kicker="WEATHER ALERTS" title="Weather changed the plan" tasks={weather} defaultOpen={weather.length>0} emptyText={filter==='All'?'No weather alerts are affecting the garden.':''} {...rowProps}/><TaskSection id="today" kicker="DO TODAY" title="Due today" tasks={dueToday} {...rowProps}/><TaskSection id="upcoming" kicker="UPCOMING" title="Future work" tasks={board.sections.upcoming} {...rowProps}/>{board.completed.length>0&&<TaskSection id="history" kicker="COMPLETED, SKIPPED, OR NOT NEEDED" title="Off the active board" tasks={board.completed} {...rowProps}/>}</section></main>}
