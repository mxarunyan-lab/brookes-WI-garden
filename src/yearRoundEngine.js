import{getCropProfile}from'./data.js';
const DAY=86400000;
const daysSince=(value,now=new Date())=>!value?Infinity:Math.max(0,Math.floor((now-new Date(value))/DAY));
const startWeeks={'bell-pepper':10,'hot-pepper':10,tomato:7,cabbage:7,broccoli:7,cauliflower:7,onion:10,marigold:6,basil:6,cucumber:3,zucchini:3,'winter-squash':3,pumpkin:3};
const dateDiff=(a,b)=>Math.round((a-b)/DAY);
export function getSeasonMode(date=new Date()){const m=date.getMonth()+1;if([12,1,2].includes(m))return{id:'winter',label:'Winter garden mode',headline:'Grow indoors. Plan outside.',focus:'Indoor crops, seed inventory, grow lights, and next-season planning.'};if([3,4].includes(m))return{id:'seed-starting',label:'Seed-starting mode',headline:'Start seeds and care for seedlings.',focus:'Seeds germinating indoors, young plants under lights, potting up, and greenhouse preparation.'};if([5,6].includes(m))return{id:'transition',label:'Outdoor transition mode',headline:'Move young plants outside safely.',focus:'Frost checks, hardening off, transplanting, planting outdoors, and rain-aware watering.'};if([7,8].includes(m))return{id:'growth-harvest',label:'Growth and harvest mode',headline:'Keep it growing. Pick it on time.',focus:'Crop-specific moisture, feeding, support, pest checks, harvests, and fall crops.'};return{id:'fall-shutdown',label:'Fall and winter-prep mode',headline:'Harvest, learn, and prepare.',focus:'End-of-season choices, next-year draft planning, garlic, cleanup, seed saving, and indoor setup.'};}
const locationName=(plant,spaces)=>spaces.find(s=>s.id===plant.spaceId)?.name||'its saved growing space';
const intervalFor=(crop,plant,weather,outdoor)=>{let days=3;const moisture=String(crop?.moisture||'');if(/consistent|steady/.test(moisture))days=2;if(['lettuce','spinach','radish'].includes(crop?.id))days=1;if(['corn','tomato','cucumber','cabbage'].includes(crop?.id))days=2;if(!outdoor)days=Math.max(1,days-1);if(weather?.high>=85)days=Math.max(1,days-1);if(weather?.dailyRainChance>=70&&outdoor)days+=1;if(/flower|producing/.test(String(plant.stage).toLowerCase()))days=Math.max(1,days-1);return days};
const cropNeedsSupport=id=>['tomato','cucumber','peas','green-bean'].includes(id);
const cropNeedsRegularPicking=id=>['lettuce','spinach','basil','cucumber','zucchini','green-bean','peas','tomato','bell-pepper','hot-pepper','strawberry'].includes(id);
function plantTasks(plant,spaces,now,weather){const tasks=[],stage=String(plant.stage||'').toLowerCase(),location=locationName(plant,spaces),space=spaces.find(s=>s.id===plant.spaceId),outdoor=!['indoor','basement','hydro','greenhouse'].includes(space?.type),crop=getCropProfile(plant.cropId),interval=intervalFor(crop,plant,weather,outdoor),lastSoil=daysSince(plant.lastSoilCheck,now),lastWatered=daysSince(plant.lastWatered,now),lastFed=daysSince(plant.lastFertilized,now),stageAge=daysSince(plant.stageUpdatedAt||plant.plantedAt,now),lastHarvest=daysSince(plant.lastHarvested,now),rainLikely=weather?.dailyRainChance>=70;if(!crop)tasks.push({id:`identify-${plant.id}`,kind:'plantDetail',plant,priority:94,title:`Match ${plant.name} to a crop`,subtitle:'Choose its crop so watering, feeding, transplant, support, and harvest guidance can be tailored.',reason:'This plant has no recognized crop profile.',action:'Fix Plant',tone:'gold'});if(/seed|germinat|sprout|seedling/.test(stage)){tasks.push({id:`seedling-${plant.id}`,kind:'plantDetail',plant,priority:90,title:`Check ${plant.name} seedlings in ${location}`,subtitle:`Check moisture ${crop?.checkDepth||'near the roots'}, light distance, airflow, crowding, and whether roots need a larger pot.`,reason:'Seedlings need light, airflow, thinning, and pot-size decisions in addition to watering.',action:'Open Plant',tone:'green'});if(stageAge>=14)tasks.push({id:`potup-${plant.id}`,kind:'plantDetail',plant,priority:84,title:`Review whether ${plant.name} needs potting up`,subtitle:'Look for circling roots, stalled growth, or leaves extending well beyond the pot.',reason:`This seedling stage has been recorded for ${stageAge} days.`,action:'Review',tone:'gold'});return tasks}if(/ready|harvest/.test(stage)||(/producing|flower/.test(stage)&&cropNeedsRegularPicking(crop?.id)&&lastHarvest>=2))tasks.push({id:`harvest-${plant.id}`,kind:'plantDetail',plant,priority:96,title:`Check ${plant.name} in ${location} for harvest`,subtitle:'Look for crop-specific readiness and record what is picked so the next check recalculates.',reason:lastHarvest===Infinity?'This crop is producing and no harvest has been recorded.':`The last harvest was ${lastHarvest} days ago.`,action:'Review',tone:'gold'});if(cropNeedsSupport(crop?.id)&&stageAge>=10&&!plant.supportCheckedAt)tasks.push({id:`support-${plant.id}`,kind:'plantDetail',plant,priority:76,title:`Check support for ${plant.name}`,subtitle:`Inspect ties, stakes, cage, or trellis in ${location}.`,reason:`${crop?.name||plant.name} commonly needs support as growth increases.`,action:'Open Plant',tone:'quiet'});if(/flower|producing|growing/.test(stage)&&lastFed>=21&&!['lettuce','radish','carrot','onion','garlic'].includes(crop?.id))tasks.push({id:`feed-${plant.id}`,kind:'plantDetail',plant,priority:70,title:`Review feeding for ${plant.name}`,subtitle:'Check crop, container, and soil condition before fertilizing.',reason:lastFed===Infinity?'No feeding or amendment check is recorded.':`The last feeding record was ${lastFed} days ago.`,action:'Review',tone:'quiet'});if(stageAge>=7&&daysSince(plant.lastHealthCheck,now)>=7)tasks.push({id:`health-${plant.id}`,kind:'plantDetail',plant,priority:68,title:`Inspect ${plant.name} for pests and stress`,subtitle:'Check leaf undersides, new growth, stems, color, chewing, spots, wilting, and crowding.',reason:'A weekly visual check catches problems early.',action:'Inspect',tone:'quiet'});if(lastSoil>=interval||lastWatered>=interval+1){const depth=crop?.checkDepth||'about 1 inch',guidance=crop?.watering||'Water only if the checked root zone is becoming dry.';tasks.push({id:`soil-${plant.id}`,kind:'soil',plant,crop,priority:outdoor&&rainLikely?62:82,title:`Check ${plant.name} moisture in ${location}`,subtitle:outdoor&&rainLikely?`Rain is likely. Check ${depth} down before watering. ${guidance}`:`Check ${depth} down. ${guidance}`,reason:lastSoil===Infinity?`No soil check is recorded. This crop currently needs a check about every ${interval} day${interval===1?'':'s'}.`:`The last check was ${lastSoil} day${lastSoil===1?'':'s'} ago; current conditions suggest checking after about ${interval}.`,action:'Check Soil',tone:'green'})}if(/overwinter/.test(stage)||plant.growingGoal==='overwinter')tasks.push({id:`overwinter-${plant.id}`,kind:'plantDetail',plant,priority:72,title:`Review winter care for ${plant.name}`,subtitle:'Check light, indoor temperature, pests, and reduced watering needs.',reason:'This plant is being kept alive through winter.',action:'Review',tone:'quiet'});return tasks}
function yearPlanTasks(garden,date){const tasks=[],plan=garden.yearPlan,year=plan?.year||date.getFullYear()+1;if(!plan?.crops?.length){if(date.getMonth()>=7)tasks.push({id:'draft-next-season',kind:'navigate',target:'plan-plant',priority:74,title:`Start the ${year} garden draft`,subtitle:'Use this year’s harvests, grow-again ratings, seed packets, and bed history to build next season automatically.',reason:'Late summer and fall are the best time to capture what worked before it is forgotten.',action:'Open Planting Desk',tone:'gold'});return tasks}const transplant=new Date(year,4,20);for(const item of plan.crops.filter(x=>x.status==='definitely'||x.status==='maybe')){const crop=getCropProfile(item.cropId),weeks=startWeeks[item.cropId];if(!crop||!weeks)continue;const start=new Date(transplant.getTime()-weeks*7*DAY),until=dateDiff(start,date),seed=(garden.seeds||[]).find(s=>!s.deletedAt&&(s.cropId===item.cropId));if(until<=21&&until>7)tasks.push({id:`prepare-seed-${year}-${item.cropId}`,kind:'navigate',target:'plan-plant',priority:86,title:`Prepare to start ${crop.name} seeds`,subtitle:`The Green Bay indoor-start window is about ${until} days away. ${seed?`Use the saved ${seed.variety||seed.name} packet.`:'No matching seed packet is saved yet.'}`,reason:`${crop.name} is in the ${year} draft and usually needs about ${weeks} weeks indoors before transplanting.`,action:'Review Plan',tone:'gold'});if(until<=7&&until>=-10)tasks.push({id:`start-seed-${year}-${item.cropId}`,kind:'setupPlant',priority:98,title:`Start ${crop.name} seeds ${until<0?'now':'this week'}`,subtitle:seed?`Use ${seed.variety||seed.name} from the Seed Catalog and record the tray so reminders continue automatically.`:'Choose a packet or decide whether to buy young plants instead.',reason:`This is the calculated indoor-start window for the ${year} Green Bay garden plan.`,action:'Start Seeds',tone:'green'});if(until<-10&&until>=-35)tasks.push({id:`late-seed-${year}-${item.cropId}`,kind:'navigate',target:'plan-plant',priority:88,title:`Decide how to handle late ${crop.name}`,subtitle:'Start now, buy young plants later, move it to a fall plan, or skip it this year.',reason:'The planned seed-starting window has passed.',action:'Choose Option',tone:'gold'})}return tasks}
export function buildYearRoundTasks({garden,weather,date=new Date()}){const tasks=[],mode=getSeasonMode(date),spaces=garden.spaces||[],plants=(garden.plants||[]).filter(p=>!p.deletedAt&&!p.archived);if(!garden.profile?.setupComplete)tasks.push({id:'finish-setup',kind:'setup',priority:100,title:'Finish setting up the garden',subtitle:'Confirm the growing spaces actually in use so the app can create specific tasks.',reason:'Initial setup is incomplete.',action:'Finish Setup',tone:'green'});if(!plants.length)tasks.push({id:'setup-first-plant',kind:'setupPlant',priority:99,title:'Add what is actually growing',subtitle:'Choose from the Seed Catalog or Crop Catalog so the app can create tailored care.',reason:'The app needs a known crop identity to create specific guidance.',action:'Add Plant',tone:'green'});plants.forEach(p=>tasks.push(...plantTasks(p,spaces,date,weather)));tasks.push(...yearPlanTasks(garden,date));if(spaces.some(s=>s.type==='greenhouse'&&!s.hidden&&!s.deletedAt))tasks.push({id:'greenhouse-check',kind:'greenhouse',priority:weather?.high>=80?90:55,title:weather?.high>=80?'Check the greenhouse before it overheats':'Record greenhouse conditions',subtitle:weather?.high>=80?`Green Bay may reach ${weather.high}°. Check the actual greenhouse temperature and vent if needed.`:'Enter temperature, humidity, airflow, and anything unusual.',reason:'A greenhouse is active.',action:'Check',tone:weather?.high>=80?'gold':'quiet'});tasks.push({id:'review-planting',kind:'navigate',target:'plan-plant',priority:50,title:mode.id==='winter'?'See what can grow indoors now':'Review the Planting Desk',subtitle:mode.focus,reason:'Planting and next-season decisions change throughout the year in Green Bay.',action:'Review',tone:'gold'});return tasks.sort((a,b)=>b.priority-a.priority).slice(0,8)}
export function migrateGarden(input,starterGarden){const garden=input&&typeof input==='object'?input:starterGarden,spaces=[...(garden.spaces||[])],ensure=s=>{if(!spaces.some(i=>i.id===s.id||i.type===s.type))spaces.push(s)};ensure({id:'indoor-starts',name:'Indoor Seed-Starting Area',type:'indoor',capacity:24});ensure({id:'basement-grow',name:'Basement Indoor Crops',type:'basement',capacity:24});return{...starterGarden,...garden,profile:{...starterGarden.profile,...(garden.profile||{}),setupComplete:Boolean(garden.profile?.setupComplete||(garden.plants||[]).length),reminderLevel:garden.profile?.reminderLevel||'normal'},spaces,plants:garden.plants||[],activity:garden.activity||[],yearPlan:garden.yearPlan||null,version:8}}
