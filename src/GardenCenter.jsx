import React from'react';
import{BookOpen,ChevronRight,ClipboardList,PackageSearch,QrCode,Sprout,Warehouse}from'lucide-react';
import{cropCatalog}from'./data.js';

function Department({icon:Icon,title,subtitle,status,onClick,tone='green'}){return <button className={`garden-center-department tone-${tone}`} onClick={onClick}><span className="department-icon"><Icon/></span><span className="department-copy"><strong>{title}</strong><small>{subtitle}</small><em>{status}</em></span><ChevronRight className="department-arrow"/></button>}
const SLOGANS=['Questionable expertise since opening day.','Extremely organized seeds. Moderately organized gardeners.','Open daily, weather and motivation permitting.','Two directors. No idea where the trowel went.'];

export default function GardenCenter({garden,navigate}){const trays=(garden.trays||[]).length,lights=(garden.growLights||[]).length,packets=(garden.seedPackets||[]).filter(x=>!x.deletedAt).length,seeds=(garden.seeds||[]).filter(x=>!x.deletedAt).length,harvests=(garden.harvests||[]).length,problems=(garden.problems||[]).filter(x=>x.status!=='resolved').length,planned=(garden.yearPlan?.crops||[]).filter(x=>x.status==='definitely'||x.status==='maybe'),missing=planned.filter(x=>!(garden.seeds||[]).some(s=>!s.deletedAt&&s.cropId===x.cropId)),slogan=SLOGANS[Math.floor(Date.now()/86400000)%SLOGANS.length];return <main className="screen garden-center-screen"><section className="dark-header garden-header garden-center-header"><Sprout/><span>OUR LITTLE GARDEN STORE</span><h1>Garden Center</h1><p>Established 2024 · Green Bay, Wisconsin<br/>{slogan}</p></section><section className="garden-center-content screen-pad"><section className="garden-center-marquee"><span>OPEN FOR GARDEN BUSINESS</span><strong>Pick a department</strong><small>Everything stays connected to the same plants, beds, seed packets, and household history.</small></section><div className="garden-center-grid"><Department icon={ClipboardList} title="Chore Board" subtitle="Today, tomorrow, and completed work" status="Main household task hub" onClick={()=>navigate('chores')} tone="gold"/><Department icon={Sprout} title="Seed Counter" subtitle="Inventory, packet photos, varieties, and viability" status={`${seeds} seed record${seeds===1?'':'s'} · ${packets} packet photo${packets===1?'':'s'}`} onClick={()=>navigate('seed-tools')} tone="green"/><Department icon={Warehouse} title="Indoor Growing" subtitle="Seedlings, lights, greenhouse, and winter crops" status={`${trays} tray${trays===1?'':'s'} · ${lights} light setup${lights===1?'':'s'}`} onClick={()=>navigate('indoor')} tone="blue"/><Department icon={QrCode} title="Label Counter" subtitle="QR labels for exact beds and growing spaces" status="Single or multi-label sheets" onClick={()=>navigate('garden')} tone="rust"/><Department icon={BookOpen} title="Garden Records" subtitle="Harvests, problems, lessons, and favorites" status={`${harvests} harvest${harvests===1?'':'s'} · ${problems} active issue${problems===1?'':'s'}`} onClick={()=>navigate('memory')} tone="cream"/><Department icon={PackageSearch} title="Next-Season Supplies" subtitle="Seeds and supplies connected to the Planting Desk" status={missing.length?`${missing.length} planned crop${missing.length===1?'':'s'} may need seed`:'Current draft has no urgent seed gaps'} onClick={()=>navigate('plan-plant')} tone="gold"/></div>{planned.length>0&&<button className="next-season-strip" onClick={()=>navigate('plan-plant')}><PackageSearch/><span><strong>{planned.length} crop{planned.length===1?'':'s'} in next year’s draft</strong><small>{missing.length?`Missing saved seed for ${missing.map(x=>cropCatalog.find(c=>c.id===x.cropId)?.name).filter(Boolean).slice(0,3).join(', ')}`:'Your saved seed catalog currently covers the draft.'}</small></span><ChevronRight/></button>}</section></main>}
