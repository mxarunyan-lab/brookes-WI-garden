deMethod,
+      analysisSourceSummary: 'Front and back packet photos analyzed together.',
+      rawVisibleText: analysis.rawVisibleText,
+      usage: result.usage || [],
+    },
+    automation: {
+      ...analysis.automation,
+      source: 'seed-packet',
+      sourceEvidence: analysis.fieldEvidence?.successionGuidance || null,
+      taskCreated: false,
+    },
+    conflicts: analysis.quality.contradictoryFields.map((field) => ({field, status: 'needs-review', detectedAt: now()})),
+  };
+  next.draftStatus = 'Ready to save';
+
+  return {
+    draft: next,
+    applied,
+    preserved,
+    optionalMissing: analysis.quality.unreadableFields,
+    conflicts: analysis.quality.contradictoryFields,
+  };
+}
diff --git a/src/styles/mobile-acceptance-qr.css b/src/styles/mobile-acceptance-qr.css
index 9d00807..6515200 100644
--- a/src/styles/mobile-acceptance-qr.css
+++ b/src/styles/mobile-acceptance-qr.css
@@ -7,3 +7,5 @@ html,body,#root{min-height:100%;max-width:100%;overflow-x:hidden}body{padding:0;
 .plant-confirmation-backdrop{position:fixed;z-index:1000;inset:0;display:grid;place-items:center;padding:max(18px,env(safe-area-inset-top,0px)) 16px max(18px,env(safe-area-inset-bottom,0px));background:#031d14cc}.plant-confirmation-card{position:relative;width:min(420px,100%);padding:28px 20px 20px;border-radius:22px;background:#fffaf1;color:#173d2a;text-align:center;box-shadow:0 20px 60px #0005}.plant-confirmation-close{position:absolute;right:10px;top:10px;width:36px;height:36px;border:0;border-radius:50%;background:#e9e1d2;color:#173d2a}.plant-confirmation-icon{width:58px;height:58px;margin:0 auto 10px;border-radius:50%;background:#e7b84b;display:grid;place-items:center}.plant-confirmation-icon svg{width:30px;height:30px}.plant-confirmation-card h2{margin:7px auto 5px;font-size:24px;line-height:1.08}.plant-confirmation-card p{margin:0 0 16px;color:#676c66}.plant-confirmation-card>div{display:grid;gap:8px}.plant-confirmation-card button{justify-content:center}
 @media(max-width:390px){.public-bed-hero>.bed-cover{height:205px}.public-bed-title h1{font-size:29px}.public-bed-footer{align-items:stretch;flex-direction:column}.public-bed-footer button{justify-content:center}.label-library-copy>div{display:grid;grid-template-columns:repeat(2,minmax(0,1fr))}.label-library-copy button{justify-content:center}.modal-action-row{display:grid!important;grid-template-columns:1fr!important}.plant-confirmation-card h2{font-size:21px}.label-counter-toolbar{align-items:stretch;flex-direction:column}.label-counter-toolbar button{justify-content:center}}
 @media(max-width:340px){.public-plant-grid article{grid-template-columns:52px minmax(0,1fr)}.public-plant-art{width:50px;height:50px}.label-library-card{grid-template-columns:68px minmax(0,1fr) 10px!important}.label-library-copy>div{grid-template-columns:1fr}}
+.packet-succession-offer{margin:0 0 16px;padding:15px;border:1px solid #cbbf9f;border-radius:16px;background:#f5eddc;text-align:left}.packet-succession-offer strong{display:block;margin:3px 0 5px;font-size:17px}.packet-succession-offer p{margin:0 0 11px;color:#4e5b50}.packet-succession-offer label{display:grid;gap:5px;font-size:13px;font-weight:800}.packet-succession-offer input{width:100%;min-height:44px;border:1px solid #b7aa89;border-radius:10px;background:#fff;padding:8px 10px;font:inherit}.packet-succession-actions{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:10px}.packet-succession-actions button{min-height:44px}.packet-succession-offer.is-scheduled{border-color:#6f9a7a;background:#e8f2e8}
+@media(max-width:390px){.packet-succession-actions{grid-template-columns:1fr}.packet-succession-offer{padding:13px}}
diff --git a/src/yearRoundEngine.js b/src/yearRoundEngine.js
index 6d39b92..b2d987b 100644
--- a/src/yearRoundEngine.js
+++ b/src/yearRoundEngine.js
@@ -100,4 +100,4 @@ export function buildYearRoundTasks({garden,weather,weatherGuidance=null,date=ne
 }
 
 export function migrateGarden(input,starterGarden){const garden=input&&typeof input==='object'?input:starterGarden;return{...starterGarden,...garden,profile:{...starterGarden.profile,...(garden.profile||{})},spaces:garden.spaces||[],plants:garden.plants||[],activity:garden.activity||[],reminders:garden.reminders||[],taskHistory:garden.taskHistory||[],seedUsage:garden.seedUsage||[],environmentalRecords:garden.environmentalRecords||[],environmentalCorrections:garden.environmentalCorrections||[],weatherRecommendationHistory:garden.weatherRecommendationHistory||[],environmentSchemaVersion:1,weatherDecisionSchemaVersion:1,seedPacketSchemaVersion:2,intelligenceSchemaVersion:1,vacationSchemaVersion:1,vacationPlans:garden.vacationPlans||[],calculatorResults:garden.calculatorResults||[],version:16}}
-export function createManualReminder(data,actor='System'){const now=new Date().toISOString(),duplicateKey=data.duplicateKey||taskDuplicateKey({...data,id:data.taskId||'manual',kind:'manual'});return{id:newId('reminder'),taskId:newId('manual-task'),duplicateKey,title:String(data.title||'').trim(),taskType:data.taskType||'Check Moisture',plantId:data.plantId||'',spaceId:data.spaceId||'',priority:Number(data.priority)||60,dueDate:data.dueDate||dateKey(now),reason:String(data.reason||'').trim(),note:String(data.note||'').trim(),enabled:true,createdAt:now,updatedAt:now,createdBy:actor,updatedBy:actor,revision:1,deletedAt:null}}
+export function createManualReminder(data,actor='System'){const now=new Date().toISOString(),duplicateKey=data.duplicateKey||taskDuplicateKey({...data,id:data.taskId||'manual',kind:'manual'});return{id:newId('reminder'),schemaVersion:1,operationId:data.operationId||`reminder:${duplicateKey}`,taskId:data.taskId||newId('manual-task'),duplicateKey,title:String(data.title||'').trim(),taskType:data.taskType||'Check Moisture',plantId:data.plantId||'',spaceId:data.spaceId||'',sourcePacketId:data.sourcePacketId||'',sourceSuccessionId:data.sourceSuccessionId||'',sourceRecommendationId:data.sourceRecommendationId||'',source:data.source||'manual',priority:Number(data.priority)||60,dueDate:data.dueDate||dateKey(now),reason:String(data.reason||'').trim(),note:String(data.note||'').trim(),enabled:true,status:'scheduled',createdAt:now,updatedAt:now,createdBy:actor,updatedBy:actor,revision:1,deletedAt:null}}
diff --git a/test/seedPacketOneTapTrust.test.js b/test/seedPacketOneTapTrust.test.js
index 84b347a..c18748c 100644
--- a/test/seedPacketOneTapTrust.test.js
+++ b/test/seedPacketOneTapTrust.test.js
@@ -3,6 +3,6 @@ import assert from'node:assert/strict';
 import{OFFICIAL_SEED_PRODUCTS,approveExactSeedProduct}from'../src/seedProductLookup.js';
 const iceberg=OFFICIAL_SEED_PRODUCTS.find(row=>row.variety==='Iceberg A');const packetMeta=(confidence='Medium')=>({source:'packet',confidence});
 test('Iceberg A approval removes malformed OCR and applies official record in one step',()=>{const draft={name:'Lettuce',brand:'Burpee',variety:'Iceberg A FULL SUN',productName:'Iceberg A FULL SUN Lettuce Seeds',directSowGuidance:'880 OUTDOOR SOWN',waterGuidance:'da keep evenly moist.',soilGuidance:'2 – SOW in average soil in full sun in DEPTH',containerSuitability:'Seedlings 5 Container',frontPhoto:'front',backPhoto:'back',fieldMeta:{name:packetMeta('High'),brand:packetMeta('High'),variety:packetMeta(),productName:packetMeta(),directSowGuidance:packetMeta(),waterGuidance:packetMeta(),soilGuidance:packetMeta(),containerSuitability:packetMeta()},packetIntelligence:{}};const next=approveExactSeedProduct(draft,{...iceberg,match:{exact:true,score:104}});assert.equal(next.variety,'Iceberg A');assert.equal(next.productName,'Iceberg A Lettuce Seeds');assert.equal(next.directSowGuidance,iceberg.guideFields.directSowGuidance);assert.equal(next.waterGuidance,iceberg.guideFields.waterGuidance);assert.equal(next.soilGuidance,iceberg.guideFields.soilGuidance);assert.equal(next.containerSuitability,iceberg.guideFields.containerSuitability);assert.equal(next.packetIntelligence.conflicts.length,0);assert.ok(next.packetInt