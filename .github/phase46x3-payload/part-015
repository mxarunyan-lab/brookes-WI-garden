ode="whatsnew" garden={garden} navigate={navigate} onUpdateProfile={updateProfile}/>} {page==='admin-support'&&<AdminScreen mode="support" garden={garden} navigate={navigate} onUpdateProfile={updateProfile}/>} {page==='admin-about'&&<AdminScreen mode="about" garden={garden} navigate={navigate} onUpdateProfile={updateProfile}/>} {page==='indoor'&&<IndoorCenter garden={garden} navigate={navigate} addTray={addTray} addLight={addLight} toggleLight={toggleLight} startHardening={startHardening} advanceHardening={advanceHardening} saveHydroPod={saveHydroPod} setModal={setModal}/>} {page==='memory'&&<GardenMemory garden={garden} navigate={navigate} resolveProblem={resolveProblem}/>} {page==='seed-tools'&&<SeedDepartmentBoundary onBack={()=>navigate('center')}><SeedTools garden={garden} navigate={navigate} savePacket={savePacket} deletePacket={deletePacket} onAddPacketQuantity={addPacketQuantity} mode="inventory"/></SeedDepartmentBoundary>} {page==='seed-labels'&&<LabelCounter garden={garden} navigate={navigate} onUpdateLabelStatus={updateQrLabel} initialTab="custom"/>} {page==='labels'&&<LabelCounter garden={garden} navigate={navigate} onUpdateLabelStatus={updateQrLabel}/>} <BottomNav page={navPage} navigate={navigate}/>{controlModal&&<GardenControls modal={modal} garden={garden} weather={weatherState.weather} close={()=>setModal(null)} savePlant={savePlant} archivePlant={archivePlant} deletePlant={deletePlant} saveHarvest={saveHarvest} saveProblem={saveProblem} onHardeningAction={hardeningAction} startHardening={startHardening}/>} {modal&&!controlModal&&<ClearDetailModal modal={modal} close={()=>setModal(null)} garden={garden} addPlant={addPlant} addSpace={addSpace} recordSoilCheck={recordSoilCheck} recordWatered={recordWatered} logGreenhouseCheck={logGreenhouseCheck} setModal={setModal} completeTask={id=>markDaily(id,'done')} snoozeTask={snoozeTask} skipTask={skipTask}/>} {confirmation&&<PlantConfirmation data={confirmation} onClose={()=>setConfirmation(null)} onView={viewConfirmedBed} onAdd={addAnother} onScheduleSuccession={schedulePacketSuccession} onDeclineSuccession={declinePacketSuccession}/>} {toast&&<div className="toast">{toast}</div>}</div></div>;
 }
diff --git a/src/SeedTools.jsx b/src/SeedTools.jsx
index 52672aa..f8bd561 100644
--- a/src/SeedTools.jsx
+++ b/src/SeedTools.jsx
@@ -2,7 +2,7 @@ import React,{useEffect,useMemo,useRef,useState}from'react';
 import{AlertTriangle,Camera,Check,CheckCircle2,ChevronDown,ExternalLink,FileText,Globe2,ImagePlus,PackageSearch,RefreshCw,RotateCw,ScanText,Sprout,Tags,Trash2,Upload,X}from'lucide-react';
 import{SecondaryHero}from'./SecondaryUI.jsx';
 import{inferCropId}from'./seedToHarvest.js';
-import{applyPacketExtraction,clearPacketDraft,compressPacketImage,emptyPacketDraft,extractionReview,findLikelyDuplicatePacket,loadPacketDraft,markManualField,PACKET_FIELD_LABELS,packetPayload,readPacketPhoto,rotatePacketImage,savePacketDraft,validatePacketImage}from'./seedPacketIntelligence.js';
+import{clearPacketDraft,compressPacketImage,emptyPacketDraft,extractionReview,findLikelyDuplicatePacket,loadPacketDraft,markManualField,PACKET_FIELD_LABELS,packetPayload,rotatePacketImage,savePacketDraft,validatePacketImage}from'./seedPacketIntelligence.js';
 import{fieldSourceLabel}from'./connectedIntelligence.js';
 import{applySeedProductCandidate,clearSeedProductMatch,lookupSeedProducts,resolveSeedConflict,seedLookupIdentity}from'./seedProductLookup.js';
 import{analyzePacketPhotos,getSeedVisionHealth}from'./seedPacketVisionClient.js';
@@ -18,8 +18,8 @@ function PacketField({label,name,draft,onChange,children,required=false,placehol
  return <label htmlFor={id} className={meta?.confidence==='Low'?'packet-field needs-review':''}><span>{label}{required?' *':''}</span>{children||<input id={id} name={name} type={type} min={min} step={step} inputMode={inputMode} required={required} value={fieldValue(draft[name])} onChange={event=>onChange(name,event.target.value)} placeholder={placeholder}/>} {status&&<small className="packet-field-source">{status}</small>}</label>;
 }
 
-function PhotoPanel({side,photo,status,progress,error,warnings=[],onSelect,onRotate,onRemove,onRead}){
- const inputId=`packet-${side}-photo`,title=side==='front'?'Front of packet':'Back of packet',description=side==='front'?'Usually identifies the crop, variety, and brand.':'Usually contains planting and growing instructions.';
+function PhotoPanel({side,photo,status,error,warnings=[],onSelect,onRotate,onRemove}){
+ const inputId=`packet-${side}-photo`,title=side==='front'?'Front of packet':'Back of packet',description=side==='front'?'Capture the complete front of the packet, edge to edge.':'Capture the complete back of the packet, edge to edge.';
  return <article className={`packet-photo-panel ${photo?'has-photo':''}`}><div className="packet-photo-heading"><span><small>{side.toUpperCase()} PHOTO</small><strong>{title}</strong><em>{description}</em></span>{photo&&<span className={`packet-photo-state state-${status?.replace(/\s+/g,'-').toLowerCase()}`}>{status}</span>}</div><label htmlFor={inputId} className="packet-photo-drop">{photo?<img src={photo} alt={`${title} preview`}/>:<><Camera/><strong>Take or choose {side} photo</strong><small>JPEG, PNG, or WebP</small></>}<input id={inputId} type="file" accept="image/jpeg,image/png,image/webp" capture="environment" onChange={event=>{const file=event.target.files?.[0];if(file)onSelect(file);event.target.value=''}}/></label>{status==='Analyzing packet'&&<div className="packet-processing" role="status" aria-live="polite"><ScanText/><span><strong>Analyzing front and back together</strong><small>Building a verified packet record.</small></span></div>}{error&&<p className="packet-photo-error" role="alert"><AlertTriangle/>{error}</p>}{warnings.length>0&&<ul className="packet-photo-warnings">{warnings.map(item=><li key={item}>{item}</li>)}</ul>}{photo&&<div className="packet-photo-actions"><label htmlFor={inputId} className="button-like"><RefreshCw/>Retake</label><button type="button" onClick={onRotate} aria-label={`Rotate ${title} clockwise`}><RotateCw/>Rotate</button><button type="button" onClick={onRemove} aria-label={`Remove ${title}`}><Trash2/>Remove</button></div>}</article>;
 }
 
@@ -39,7 +39,7 @@ function OnlineLookupPanel({draft,lookup,onChoose,onRetry,onResolve,onChangeMatc
 
 
 export default function SeedTools({garden,navigate,savePacket,deletePacket,onAddPacketQuantity}){
- const prefill=useMemo(readPrefill,[]),recovered=useMemo(loadPacketDraft,[]),[draft,setDraft]=useState(()=>emptyPacketDraft(prefill)),[frontPhoto,setFrontPhoto]=useState(''),[backPhoto,setBackPhoto]=useState(''),[imageMeta,setImageMeta]=useState({front:null,back:null}),[photoState,setPhotoState]=useState({front:{status:'Photo ready',progress:0,error:'',warnings:[]},back:{status:'Photo ready',progress:0,error:'',warnings:[]}}),[recovery,setRecovery]=useState(()=>hasDraftContent(recovered)?recovered:null),[saving,setSaving]=useState(false),[saveError,setSaveError]=useState(''),[savedPacket,setSavedPacket]=useState(null),[allowSeparate,setAllowSeparate]=useState(false),[visionState,setVisionState]=useState({status:'idle',message:'',requestId:'',configured:null}),[lookup,setLookup]=useState({status:'idle',candidates:[],otherCandidates:[],primaryCandidate:null,error:'',liveVerification:null}),processed=useRef(new Set()),lookedUp=useRef(''),applyingMatch=useRef(false),saveAttempt=useRef(false),reviewSection=useRef(null),savedSection=useRef(null);
+ const prefill=useMemo(readPrefill,[]),recovered=useMemo(loadPacketDraft,[]),[draft,setDraft]=useState(()=>emptyPacketDraft(prefill)),[frontPhoto,setFrontPhoto]=useState(''),[backPhoto,setBackPhoto]=useState(''),[imageMeta,setImageMeta]=useState({front:null,back:null}),[photoState,setPhotoState]=useState({front:{status:'Photo ready',progress:0,error:'',warnings:[]},back:{status:'Photo ready',progress:0,error:'',warnings:[]}}),[recovery,setRecovery]=useState(()=>hasDraftContent(recovered)?recovered:null)