name: Phase 4.8.3c Live Verification (archived)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Phase 4.8.3c live verification is archived. Current releases use the shared production verifier."
