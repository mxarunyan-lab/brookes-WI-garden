Extraction,clearPacketDraft,compressPacketImage,emptyPacketDraft,extractionReview,findLikelyDuplicatePacket,loadPacketDraft,markManualField,PACKET_FIELD_LABELS,packetPayload,readPacketPhoto,rotatePacketImage,savePacketDraft,validatePacketImage}from'./seedPacketIntelligence.js';
+import{fieldSourceLabel}from'./connectedIntelligence.js';
+import{applySeedProductCandidate,lookupSeedProducts,resolveSeedConflict,seedLookupIdentity}from'./seedProductLookup.js';
 
 const readPrefill=()=>{try{const raw=sessionStorage.getItem('seed-inventory-prefill');sessionStorage.removeItem('seed-inventory-prefill');return raw?JSON.parse(raw):{}}catch{return{}}};
 const hasDraftContent=snapshot=>Boolean(snapshot&&(snapshot.frontPhoto||snapshot.backPhoto||Object.entries(snapshot.draft||{}).some(([key,value])=>!['packetYear','reservedQuantity','fieldMeta','packetIntelligence','draftStatus'].includes(key)&&value!==''&&value!==null&&value!==undefined&&(!Array.isArray(value)||value.length))));
 const fieldValue=value=>Array.isArray(value)?value.join(', '):value??'';
-const fieldStatus=meta=>!meta?'':meta.source==='manual'?(meta.manuallyCorrected?'Manually corrected':'Manual entry'):meta.inferred?'Inferred from packet context':`${meta.confidence||'Low'} confidence · read from ${meta.sourcePhoto||'packet'}`;
+const fieldStatus=meta=>!meta?'':`${meta.confidence||'Low'} · ${fieldSourceLabel(meta)}${meta.sourcePhoto?` · ${meta.sourcePhoto} photo`:''}`;
 
 function PacketField({label,name,draft,onChange,children,required=false,placeholder='',type='text',min,step,inputMode}){
  const meta=draft.fieldMeta?.[name],status=fieldStatus(meta),id=`packet-${name}`;
@@ -25,16 +27,22 @@ function ReviewSummary({draft}){
  return <section className="packet-review-summary" aria-live="polite">{review.ready.length>0&&<><div><CheckCircle2/><span><small>READY</small><strong>{review.ready.length} confirmed detail{review.ready.length===1?'':'s'}</strong></span></div><ul>{review.ready.slice(0,5).map(item)}</ul></>}{review.check.length>0&&<details open><summary><AlertTriangle/><span><small>PLEASE CHECK</small><strong>{review.check.length} uncertain detail{review.check.length===1?'':'s'}</strong></span><ChevronDown/></summary><ul>{review.check.map(item)}</ul></details>}{review.missing.length>0&&<details><summary><FileText/><span><small>NOT FOUND</small><strong>{review.missing.length} useful field{review.missing.length===1?'':'s'} left blank</strong></span><ChevronDown/></summary><ul>{review.missing.map(row=><li key={row.key}>{PACKET_FIELD_LABELS[row.key]||row.key}</li>)}</ul></details>}</section>;
 }
 
+function OnlineLookupPanel({draft,lookup,onChoose,onRetry,onResolve}){const conflicts=(draft.packetIntelligence?.conflicts||[]).filter(row=>row.status!=='resolved'),exact=draft.packetIntelligence?.onlineLookup;if(lookup.status==='idle'&&!exact&&!conflicts.length)return null;return <section className="packet-online-lookup"><div className="packet-form-section-title"><Globe2/><span><small>EXACT PRODUCT LOOKUP</small><strong>{lookup.status==='searching'?'Searching official seed sources…':exact?`Matched ${draft.productName||`${draft.variety} ${draft.name}`}`:'Review possible products'}</strong></span></div>{lookup.status==='searching'&&<p><RefreshCw className="is-spinning"/> Comparing brand, crop, variety, product ID, barcode, and packet wording.</p>}{lookup.error&&<p className="packet-photo-error"><AlertTriangle/>{lookup.error}</p>}{lookup.candidates?.length>0&&<div className="packet-product-candidates">{lookup.candidates.map(product=><article key={product.id}><span><strong>{product.productName}</strong><small>{product.brand} · match score {product.match.score} · {product.match.reasons.join(', ')}</small><small>{product.sourceName}</small></span><div><button type="button" onClick={()=>onChoose(product)}>{product.match.exact?'Use exact match':'Use this candidate'}</button><a href={product.sourceUrl} target="_blank" rel="noreferrer"><ExternalLink/>Official source</a></div></article>)}</div>}{exact&&<p className="packet-lookup-source"><CheckCircle2/><span><strong>Online values are source-labeled.</strong><small>{exact.sourceName}{lookup.liveVerification?.reachable?' · official page reached now':' · cached official product record; source link retained'}</small></span><a href={exact.sourceUrl} target="_blank" rel="noreferrer"><ExternalLink/>View source</a></p>}{lookup.status==='none'&&<p>No exact official match was found. Nothing generic was inserted as exact packet data.</p>}{conflicts.length>0&&<div className="packet-conflict-list"><strong>Packet and online source conflicts</strong>{conflicts.map(conflict=><article key={conflict.field}><span><small>{PACKET_FIELD_LABELS[conflict.field]||conflict.field}</small><b>Keep current: {String(conflict.activeValue)}</b><em>Online: {String(conflict.onlineValue)}</em></span><div><button type="button" onClick={()=>onResolve(conflict.field,'active')}>Keep packet / correction</button><button type="button" onClick={()=>onResolve(conflict.field,'online')}>Use online value</button></div></article>)}</div>}<button type="button" className="secondary-button" onClick={onRetry} disabled={lookup.status==='searching'}><RefreshCw/>Search official sources again</button></section>}
+
 export default function SeedTools({garden,navigate,savePacket,deletePacket,onAddPacketQuantity}){
- const prefill=useMemo(readPrefill,[]),recovered=useMemo(loadPacketDraft,[]),[draft,setDraft]=useState(()=>emptyPacketDraft(prefill)),[frontPhoto,setFrontPhoto]=useState(''),[backPhoto,setBackPhoto]=useState(''),[imageMeta,setImageMeta]=useState({front:null,back:null}),[photoState,setPhotoState]=useState({front:{status:'Photo ready',progress:0,error:'',warnings:[]},back:{status:'Photo ready',progress:0,error:'',warnings:[]}}),[recovery,setRecovery]=useState(()=>hasDraftContent(recovered)?recovered:null),[saving,setSaving]=useState(false),[saveError,setSaveError]=useState(''),[savedPacket,setSavedPacket]=useState(null),[allowSeparate,setAllowSeparate]=useState(false),processed=useRef(new Set()),saveAttempt=useRef(false),savedSection=useRef(null);
+ const prefill=useMemo(readPrefill,[]),recovered=useMemo(loadPacketDraft,[]),[draft,setDraft]=useState(()=>emptyPacketDraft(prefill)),[frontPhoto,setFrontPhoto]=useState(''),[backPhoto,setBackPhoto]=useState(''),[imageMeta,setImageMeta]=useState({front:null,back:null}),[photoState,setPhotoState]=useState({front:{status:'Photo ready',progress:0,error:'',warnings:[]},back:{status:'Photo ready',progress:0,error:'',warnings:[]}}),[recovery,setRecovery]=useState(()=>hasDraftContent(recovered)?recovered:null),[saving,setSaving]=useState(false),[saveError,setSaveError]=useState(''),[savedPacket,setSavedPacket]=useState(null),[allowSeparate,setAllowSeparate]=useState(false),[lookup,setLookup]=useState({status:'idle',candidates:[],error:'',liveVerification:null}),processed=useRef(new Set()),lookedUp=useRef(''),saveAttempt=useRef(false),savedSection=useRef(null);
  const packets=(garden.seedPackets||[]).filter(packet=>!packet.deletedAt),duplicate=useMemo(()=>allowSeparate?null:findLikelyDuplicatePacket(draft,packets),[allowSeparate,draft,packets]);
 
  useEffect(()=>{const timer=setTimeout(()=>{if(savedPacket)return;const result=savePacketDraft({draft,frontPhoto,backPhoto,imageMeta,photoState});if(!result.ok)setSaveError('The temporary packet draft could not be stored, but the current form is still available on screen.')},350);return()=>clearTimeout(timer)},[draft,frontPhoto,backPhoto,imageMeta,photoState,savedPacket]);
+ useEffect(()=>{const identity=seedLookupIdentity(draft),key=[identity.brand,identity.crop,identity.variety,identity.barcode,identity.sku].join('|').toLowerCase();if(!identity.brand||!identity.crop||(!identity.variety&&!identity.barcode&&!identity.sku)||key===lookedUp.current)return;const timer=setTimeout(async()=>{lookedUp.current=key;setLookup({status:'searching',candidates:[],error:'',liveVerification:null});try{const result=await lookupSeedProducts(identity,{verifyOnline:false});setLookup({status:result.exactCandidate?'matched':result.candidates.length?'candidates':'none',candidates:result.candidates,error:'',liveVerification:result.liveVerification});if(result.exactCandidate)setDraft(current=>applySeedProductCandidate(current,result.exactCandidate))}catch(error){console.error('[Runyan Garden] exact packet lookup failed',error);setLookup({status:'error',candidates:[],error:'Exact-product lookup could not finish. Packet photos and manual entry remain available.',liveVerification:null})}},550);return()=>clearTimeout(timer)},[draft.brand,draft.name,draft.variety,draft.barcode,draft.officialProductId]);
 
  const updateField=(key,value)=>{setDraft(current=>markManualField(current,key,key==='designations'?String(value).split(',').map(item=>item.trim()).filter(Boolean):value));setSavedPacket(null);setSaveError('')};
  const setSideState=(side,patch)=>setPhotoState(current=>({...current,[side]:{...current[side],...patch}}));
  const photoFor=side=>side==='front'?frontPhoto:backPhoto;
  const setPhotoFor=(side,value)=>side==='front'?setFrontPhoto(value):setBackPhoto(value);
+ const chooseOnlineProduct=product=>{setDraft(current=>applySeedProductCandidate(current,product));setLookup(current=>({...current,status:'matched'}))};
+ const resolveConflict=(field,choice)=>setDraft(current=>resolveSeedConflict(current,field,choice));
+ const retryLookup=async()=>{setLookup({status:'searching',candidates:[],error:'',liveVerification:null});try{const result=await lookupSeedProducts(seedLookupIdentity(draft),{ignoreCache:true,verifyOnline:false});setLookup({status:result.exactCandidate?'matched':result.candidates.length?'candidates':'none',candidates:result.candidates,error:'',liveVerification:result.liveVerification});if(result.exactCandidate)setDraft(current=>applySeedProductCandidate(current,result.exactCandidate))}catch(error){console.error('[Runyan Garden] exact packet lookup retry failed',error);setLookup({status:'error',candidates:[],error:'Exact-product lookup could not finish. Continue with packet text or manual entry.',liveVerification:null})}};
 
  const processPhoto=async(side,dataUrl,force=false)=>{const key=`${side}:${dataUrl.length}:${dataUrl.slice(-80)}`;if(!force&&processed.current.has(key))return;processed.current.add(key);setSideState(side,{status:'Reading packet',progress:0,error:''});try{const extraction=await readPacketPhoto(dataUrl,{side,onProgress:message=>setSideState(side,{status:'Reading packet',progress:message.progress||0})});setDraft(current=>applyPacketExtraction(current,extraction));setSideState(side,{status:side==='front'?'Front packet read':'Back packet read',progress:1,error:''})}catch(error){console.error('[Runyan Garden] packet extraction failure',{side,kind:error?.kind,error});const message=error?.kind==='timeout'?'Packet reading took too long. Keep the photo, retake it, or enter the details manually.':error?.kind==='unreadable'?'The photo was saved, but the text was not clear enough to prefill details. Retake it or continue manually.':'The packet reader could not process this photo. The photo and form are still available.';setSideState(side,{status:'Could not read this photo',progress:0,error:message})}};
  const selectPhoto=async(side,file)=>{const validation=validatePacketImage(file);if(!validation.ok){setSideState(side,{status:'Could not read this photo',error:validation.message});return}setSideState(side,{status:'Processing photo',progress:0,error:'',warnings:[]});try{const compressed=await compressPacketImage(file);setPhotoFor(side,compressed.dataUrl);setImageMeta(current=>({...current,[side]:{width:compressed.width,height:compressed.height,originalBytes:compressed.originalBytes,compressedBytes:compressed.compressedBytes,mimeType:compressed.mimeType}}));setSideState(side,{status:'Photo ready',progress:0,error:'',warnings:compressed.qualityWarnings});await processPhoto(side,compressed.dataUrl)}catch(error){console.error('[Runyan Garden] packet image 