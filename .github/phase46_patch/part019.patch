s.forEach(plant=>tasks.push(...plantTasks({...plant},spaces,garden,date,weather)));
- tasks.push(...weatherTasks(weather,date,garden,weatherGuidance),...manualTasks(garden,date),...seasonalTasks(garden,date));
+ tasks.push(...weatherTasks(weather,date,garden,weatherGuidance),...manualTasks(garden,date),...seasonalTasks(garden,date),...vacationTasksForBoard(garden));
  if(spaces.some(space=>space.type==='greenhouse'&&!space.hidden&&!space.deletedAt))tasks.push(task({id:'greenhouse-check',kind:'greenhouse',taskType:weather?.signals?.greenhouseOverheatRisk?.status?'Greenhouse Ventilation':'Check Moisture',priority:weather?.signals?.greenhouseOverheatRisk?.status?94:55,title:weather?.signals?.greenhouseOverheatRisk?.status?'Ventilate and inspect the greenhouse':'Record greenhouse conditions',subtitle:'Check temperature, humidity, airflow, and anything unusual. Outdoor rain does not count as greenhouse watering.',reason:weather?.signals?.greenhouseOverheatRisk?.reason||'A greenhouse can differ sharply from outside weather.',action:'Check'},date));
- const unique=new Map();tasks.forEach(item=>{const current=unique.get(item.id);if(!current||item.priority>current.priority)unique.set(item.id,item)});
+ const unique=new Map();tasks.forEach(item=>{const key=taskDuplicateKey(item),current=unique.get(key);if(!current||item.priority>current.priority)unique.set(key,{...item,duplicateKey:key})});
  return[...unique.values()].sort((a,b)=>String(a.dueDate).localeCompare(String(b.dueDate))||b.priority-a.priority).slice(0,80);
 }
 
-export function migrateGarden(input,starterGarden){const garden=input&&typeof input==='object'?input:starterGarden;return{...starterGarden,...garden,profile:{...starterGarden.profile,...(garden.profile||{})},spaces:garden.spaces||[],plants:garden.plants||[],activity:garden.activity||[],reminders:garden.reminders||[],taskHistory:garden.taskHistory||[],seedUsage:garden.seedUsage||[],environmentalRecords:garden.environmentalRecords||[],environmentalCorrections:garden.environmentalCorrections||[],weatherRecommendationHistory:garden.weatherRecommendationHistory||[],environmentSchemaVersion:1,weatherDecisionSchemaVersion:1,seedPacketSchemaVersion:1,version:15}}
-export function createManualReminder(data,actor='System'){const now=new Date().toISOString();return{id:newId('reminder'),taskId:newId('manual-task'),title:String(data.title||'').trim(),taskType:data.taskType||'Check Moisture',plantId:data.plantId||'',spaceId:data.spaceId||'',priority:Number(data.priority)||60,dueDate:data.dueDate||dateKey(now),reason:String(data.reason||'').trim(),note:String(data.note||'').trim(),enabled:true,createdAt:now,updatedAt:now,createdBy:actor,updatedBy:actor,revision:1,deletedAt:null}}
+export function migrateGarden(input,starterGarden){const garden=input&&typeof input==='object'?input:starterGarden;return{...starterGarden,...garden,profile:{...starterGarden.profile,...(garden.profile||{})},spaces:garden.spaces||[],plants:garden.plants||[],activity:garden.activity||[],reminders:garden.reminders||[],taskHistory:garden.taskHistory||[],seedUsage:garden.seedUsage||[],environmentalRecords:garden.environmentalRecords||[],environmentalCorrections:garden.environmentalCorrections||[],weatherRecommendationHistory:garden.weatherRecommendationHistory||[],environmentSchemaVersion:1,weatherDecisionSchemaVersion:1,seedPacketSchemaVersion:2,intelligenceSchemaVersion:1,vacationSchemaVersion:1,vacationPlans:garden.vacationPlans||[],calculatorResults:garden.calculatorResults||[],version:16}}
+export function createManualReminder(data,actor='System'){const now=new Date().toISOString(),duplicateKey=data.duplicateKey||taskDuplicateKey({...data,id:data.taskId||'manual',kind:'manual'});return{id:newId('reminder'),taskId:newId('manual-task'),duplicateKey,title:String(data.title||'').trim(),taskType:data.taskType||'Check Moisture',plantId:data.plantId||'',spaceId:data.spaceId||'',priority:Number(data.priority)||60,dueDate:data.dueDate||dateKey(now),reason:String(data.reason||'').trim(),note:String(data.note||'').trim(),enabled:true,createdAt:now,updatedAt:now,createdBy:actor,updatedBy:actor,revision:1,deletedAt:null}}
