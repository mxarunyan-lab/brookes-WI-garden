d}));
+ },[defaultSpaceId,tray.spaceId]);
+
+ useEffect(()=>{
+  try{
+   const raw=sessionStorage.getItem('indoor-tray-prefill');
+   if(!raw)return;
+   const prefill=JSON.parse(raw);
+   sessionStorage.removeItem('indoor-tray-prefill');
+   setTray(current=>({...current,...prefill,sourcePacketId:prefill.sourcePacketId||prefill.packetId||current.sourcePacketId,spaceId:prefill.spaceId||current.spaceId||defaultSpaceId,cells:Number(prefill.cells||prefill.quantity||current.cells)||12,seedsUsed:Number(prefill.seedsUsed||prefill.quantity||current.seedsUsed)||12,startedAt:prefill.startedAt||current.startedAt}));
+   setTrayOpen(true);
+  }catch(error){console.error('[Runyan Garden] indoor tray prefill could not be restored',error)}
+ },[defaultSpaceId]);
+
+ const selectPacket=packetId=>{
+  const packet=packets.find(row=>row.id===packetId);
+  if(!packet){setTray(current=>({...current,sourcePacketId:''}));return}
+  setTray(current=>({...current,sourcePacketId:packet.id,cropId:packet.cropId||current.cropId,crop:packet.name||current.crop,variety:packet.variety||'',seedCountType:packet.countType||current.seedCountType,cells:Math.min(Math.max(1,Number(current.cells)||12),Math.max(1,packetAvailable(packet))),seedsUsed:Math.min(Math.max(1,Number(current.seedsUsed)||12),Math.max(1,packetAvailable(packet)))}));
+ };
+ const submitTray=event=>{
+  event.preventDefault();
+  const saved=addTray?.({...tray,cells:Number(tray.cells)||1,seedsUsed:Math.max(1,Number(tray.seedsUsed||tray.cells)||1)});
+  if(saved===false)return;
+  setTray(emptyTray(defaultSpaceId));
+  setTrayOpen(false);
+ };
+
+ const indoorPlants=useMemo(()=>garden.plants.filter(plant=>{const space=garden.spaces.find(row=>row.id===plant.spaceId);return!plant.deletedAt&&!plant.archived&&['indoor','basement','greenhouse','hydro'].includes(space?.type)}),[garden.plants,garden.spaces]);
+ const hardening=garden.hardeningPlans||[],latestReading=garden.greenhouseReadings?.[0],outdoorCandidates=indoorPlants.filter(plant=>plant.growingGoal==='spring-seedling'&&['Seedling','Growing'].includes(plant.stage)&&!hardening.some(plan=>plan.plantId===plant.id&&!plan.complete));
+
+ return <main className="screen indoor-center-screen"><section className="dark-header garden-header indoor-center-header"><button className="back-button" onClick={()=>navigate('back')} aria-label="Go back"><ArrowLeft/></button><Sprout/><span>YEAR-ROUND GROWING</span><h1>Indoor Growing</h1><p>Start from a saved packet when possible. The tray, seed use, expected emergence, care guidance, and Plant Journey then stay connected.</p></section><section className="indoor-center-content screen-pad">
+  {indoorPlants.length>0&&<article className="control-center-card"><div className="control-center-title"><LampDesk/><div><span className="section-kicker">SUGGESTED FROM YOUR PLANTS</span><h2>Light and next-step recommendations</h2></div></div><div className="center-list">{indoorPlants.map(plant=>{const suggestion=lightSuggestion(plant),assigned=garden.growLights?.find(row=>row.id===plant.growLightId);return <div key={plant.id}><span><strong>{plant.name}</strong><small>{assigned?`${assigned.name}: ${assigned.onTime}–${assigned.offTime}`:`Suggested ${suggestion.on}–${suggestion.off} (${suggestion.hours} hours)`} · {suggestion.why}</small></span><em>{plant.stage}</em></div>})}</div></article>}
+
+  <article className="control-center-card indoor-tray-card"><div className="control-center-title"><Sprout/><div><span className="section-kicker">SEEDLING TRAYS</span><h2>{garden.trays?.length||0} active trays</h2></div><button onClick={()=>setTrayOpen(value=>!value)}><Plus/> Add</button></div>
+   {trayOpen&&<form onSubmit={submitTray} className="inline-control-form connected-tray-form"><label>Use a saved seed packet<select value={tray.sourcePacketId} onChange={event=>selectPacket(event.target.value)}><option value="">No packet selected</option>{packets.map(packet=><option key={packet.id} value={packet.id}>{packetDisplay(packet)} · {packetAvailable(packet)} available</option>)}</select></label>{selectedPacket?<section className="exact-packet-guidance"><div><PackageSearch/><span><small>EXACT PACKET GUIDANCE</small><strong>{packetDisplay(selectedPacket)}</strong></span></div>{packetGuidance(selectedPacket).length?<dl>{packetGuidance(selectedPacket).map(([label,value])=><div key={label}><dt>{label}</dt><dd>{value}</dd></div>)}</dl>:<p>This saved packet identifies the crop and variety, but no growing instructions were recorded on it.</p>}</section>:<p className="honest-guidance-note">No packet is linked. The tray will be saved from your manual entries without claiming exact packet guidance.</p>}<div className="two-field-row"><label>Tray name<input value={tray.name} onChange={event=>setTray({...tray,name:event.target.value})} required/></label><label>Started on<input type="date" value={tray.startedAt} onChange={event=>setTray({...tray,startedAt:event.target.value})} required/></label></div><div className="two-field-row"><label>Crop<input value={tray.crop} onChange={event=>setTray({...tray,crop:event.target.value})} required/></label><label>Variety<input value={tray.variety} onChange={event=>setTray({...tray,variety:event.target.value})}/></label></div><label>Indoor location<select value={tray.spaceId} onChange={event=>setTray({...tray,spaceId:event.target.value})}><option value="">Unassigned</option>{indoorSpaces.map(space=><option key={space.id} value={space.id}>{space.name}</option>)}</select></label><div className="two-field-row"><label>Cells planted<input type="number" min="1" value={tray.cells} onChange={event=>setTray({...tray,cells:event.target.value,seedsUsed:event.target.value})}/></label><label>Seeds used<input type="number" min="1" max={selectedPacket?packetAvailable(selectedPacket):undefined} value={tray.seedsUsed} onChange={event=>setTray({...tray,seedsUsed:event.target.value})}/></label></div><label>Stage<select value={tray.stage} onChange={event=>setTray({...tray,stage:event.target.value})}><option>Sown</option><option>Germinating</option><option>Sprouted</option><option>Ready to pot up</option></select></label><button className="primary-button">Save tray and start Plant Journey</button></form>}
+   <div className="center-list connected-tray-list">{(garden.trays||[]).filter(row=>!row.deletedAt).map(item=>{const plant=garden.plants.find(row=>row.id===item.linkedPlantId&&!row.deletedAt),packet=packets.find(row=>row.id===item.sourcePacketId);return <div key={item.id}><span><strong>{item.name}</strong><small>{[item.crop,item.variety,`${item.cells} cells`].filter(Boolean).join(' · ')}</small>{packet&&<small>Packet: {packetDisplay(packet)}</small>}{item.expectedEmergence&&<small>Expected emergence: {item.expectedEmergence}</small>}</span><em>{item.stage}</em>{plant&&<button className="icon-text-button" onClick={()=>setModal({type:'managePlant',plant,view:'journey'})}>Plant Journey <ChevronRight/></button>}</div>})}{!garden.trays?.length&&<p className="center-empty">Choose a saved packet to prefill crop, variety, exact instructions, and expected timing. Saving the tray also starts the connected Plant Journey.</p>}</div>
+  </article>
+
+  <article className="control-center-card"><div className="control-center-title"><LampDesk/><div><span className="section-kicker">GROW LIGHTS</span><h2>Accepted or custom schedules</h2></div><button onClick={()=>setLightOpen(value=>!value)}><Plus/> Add</button></div>{lightOpen&&<form onSubmit={event=>{event.preventDefault();addLight(light);setLightOpen(false)}} className="inline-control-form"><label>Name<input value={light.name} onChange={event=>setLight({...light,name:event.target.value})}/></label><div className="two-field-row"><label>On<input type="time" value={light.onTime} onChange={event=>setLight({...light,onTime:event.target.value})}/></label><label>Off<input type="time" value={light.offTime} onChange={event=>setLight({...light,offTime:event.target.value})}/></label></div><button className="primary-button">Save schedule</button></form>}<div className="center-list">{(garden.growLights||[]).map(item=><button className="light-row" key={item.id} onClick={()=>toggleLight(item.id)}><LampDesk/><span><strong>{item.name}</strong><small>{item.onTime}–{item.offTime}</small></span><em className={item.checkedToday?'is-checked':''}>{item.checkedToday?<><Check/> Checked</>:'Check today'}</em></button>)}{!garden.growLights?.length&&<p className="center-empty">Add a plant indoors first. Its crop and goal will suggest a schedule you can accept or adjust.</p>}</div></article>
+
+  <article className="control-center-card"><div className="control-center-title"><ThermometerSun/><div><span className="section-kicker">PREPARING FOR OUTDOORS</span><h2>Automatic hardening-off candidates</h2></div></div>{outdoorCandidates.map(plant=><button className="start-hardening" key={plant.id} onClick={()=>startHardening(plant.id)}><span><strong>{plant.name} is approaching its outdoor move</strong><small>Start the guided seven-day weather-aware transition.</small></span><Plus/></button>)}<div className="hardening-list">{hardening.filter(plan=>!plan.complete).map(plan=><div key={plan.id}><span><strong>{plan.plantName}: Day {plan.day} of 7</strong><small>{['1 hour in shade','2 hours in shade','3 hours with morning sun','4 hours outside','6 hours outside','Most of the day outside','Full day and overnight only if weather is safe'][plan.day-1]}</small></span><button onClick={()=>advanceHardening(plan.id)}>{plan.day===7?'Finish':'Done today'}</button></div>)}{!outdoorCandidates.length&&!hardening.some(plan=>!plan.complete)&&<p className="center-empty">The app will move spring seedlings into this section when their crop timing, stage, and Green Bay conditions line up.</p>}</div></article>
+
+  <article className="control-center-card greenhouse-center-card"><div className="control-center-title"><Warehouse/><div><span className="section-kicker">GREENHOUSE</span><h2>{latestReading?`${latestReading.temperature??'—'}°F · ${latestReading.humidity??'—'}%`:'No reading yet'}</h2></div><button onClick={()=>setModal({type:'greenhouseCheck'})}>Log check</button></div>{latestReading&&<p className="last-reading">Last checked {formatDateTime(latestReading.at)}{latestReading.note?` · ${latestReading.note}`:''}</p>}</article>
+
+  <details className="control-center-card"><summary className="control-center-title"><FlaskConical/><div><span className="section-kicker">HYDROPONICS</span><h2>Light household tracking</h2></div></summary><p className="center-empty">LetPot remains responsible for equipment alerts. This app only keeps crop, planting, harvest, and replacement timing in the shared garden record.</p><button onClick={()=>setPodOpen(value=>!value)}><Plus/> Add or update pod</button>{podOpen&&<form onSubmit={event=>{event.preventDefault();saveHydroPod(pod);setPodOpen(false)}} className="inline-control-form"><div className="two-field-row"><label>Position<input value={pod.position} onChange={event=>setPod({...pod,position:event.target.value})}/></label><label>Status<select value={pod.status} onChange={event=>setPod({...pod,status:event.target.value})}><option>Planted</option><option>Germinating</option><option>Growing</option><option>Harvesting</option><option>Empty</option></select></label></div><label>Crop<input value={pod.crop} onChange={event=>setPod({...pod,crop:event.target.value})}/></label><button className="primary-button">Save pod</button></form>}<div className="hydro-grid">{(garden.hydroPods||[]).map(item=><div key={item.id}><Droplets/><strong>Pod {item.position}</strong><span>{item.crop||'Empty'}</span><small>{item.status}</small></div>)}</div></details>
+ </section></main>;
+}
diff --git a/src/PlanPlantHub.jsx b/src/PlanPlantHub.jsx
index 9b6630cd43dbd14f664e5d89bbafc0313d6e0cd4..a63399495edf532855d410013ae373d032b43c52 100644
--- a/src/PlanPlantHub.jsx
+++ b/src/PlanPlantHub.jsx
@@ -14,6 +14,7 @@ const addDays=days=>{const date=new Date(`${todayKey()}T12:00:00`);date.setDa