small>{formatDateTime(entry.at)} · {entry.detail}</small></article>):<p className="journey-none">Watering and soil checks from the Chore Board will appear here.</p>}</JourneyAccordion>
- <JourneyAccordion title="Photos and notes" subtitle={`${photos.length} stage photo${photos.length===1?'':'s'} · private notes preserved`} icon={Camera}>{draft.notes&&<p className="journey-private-note">{draft.notes}</p>}{photos.length?<div className="journey-photos">{photos.map((entry,index)=><figure key={index}><img src={entry.photo} alt={`${entry.stage} record`}/><figcaption>{entry.stage} · {pretty(entry.date)}</figcaption></figure>)}</div>:<p className="journey-none">No stage photos have been added yet.</p>}</JourneyAccordion>
+ <JourneyAccordion title="Photos and notes" subtitle={`${photos.length} stored stage photo${photos.length===1?'':'s'} · no image analysis claimed`} icon={Camera}>{draft.notes&&<p className="journey-private-note">{draft.notes}</p>}{photos.length?<div className="journey-photos">{photos.map((entry,index)=><figure key={index}><img src={entry.photo} alt={`${entry.stage} record`}/><figcaption>{entry.stage} · {pretty(entry.date)}</figcaption></figure>)}</div>:<p className="journey-none">No stage photos have been added yet.</p>}</JourneyAccordion>
  <JourneyAccordion title="Recent completed activity" subtitle={`${completed.length} recent milestone or task record${completed.length===1?'':'s'}`} icon={CheckCircle2}>{completed.slice(0,8).map(event=><article className="journey-simple-row" key={`${event.type}-${event.id}`}><strong>{event.title}</strong><small>{formatDateTime(event.at)} · {event.classification||'Confirmed'}</small>{event.detail&&<p>{event.detail}</p>}</article>)}</JourneyAccordion></div>}
- {view==='harvest'&&<form className="modal-form" onSubmit={submitHarvest}><div className="control-callout"><Leaf/><span><strong>Record what was actually picked.</strong><small>{draft.harvestPattern?.type==='repeat'?'This is a repeat-harvest crop; one harvest will not finish the planting.':'This crop is currently treated as a one-time harvest.'}</small></span></div><div className="two-field-row"><label>Amount<input type="number" min="0" step="0.1" value={harvest.amount} onChange={event=>setHarvest({...harvest,amount:Number(event.target.value)})}/></label><label>Unit<select value={harvest.unit} onChange={event=>setHarvest({...harvest,unit:event.target.value})}><option>count</option><option>ounces</option><option>pounds</option><option>handful</option><option>bunch</option><option>basket</option></select></label></div><label>Quality<select value={harvest.quality} onChange={event=>setHarvest({...harvest,quality:event.target.value})}><option>Excellent</option><option>Good</option><option>Okay</option><option>Poor</option></select></label><label>Grow this again?<select value={harvest.growAgain} onChange={event=>setHarvest({...harvest,growAgain:event.target.value})}><option value="yes">Yes</option><option value="maybe">Maybe</option><option value="no">No</option></select></label><label>Harvest note<textarea value={harvest.note} onChange={event=>setHarvest({...harvest,note:event.target.value})}/></label><button className="primary-button" type="submit">Save harvest</button></form>}
- {view==='problem'&&<form className="modal-form" onSubmit={submitProblem}><div className="control-callout warning"><AlertTriangle/><span><strong>Log what you see, not a guess.</strong><small>Failures and removals preserve the Plant Journey instead of erasing it.</small></span></div><label>Problem type<select value={problem.type} onChange={event=>setProblem({...problem,type:event.target.value})}><option>Pest spotted</option><option>Disease concern</option><option>Frost damage</option><option>Heat damage</option><option>Animal damage</option><option>Germination failure</option><option>Transplant failure</option><option>Plant died</option><option>General issue</option></select></label><label>What should happen next?<select value={problem.severity} onChange={event=>setProblem({...problem,severity:event.target.value})}><option>Keep watching</option><option>Needs attention soon</option><option>Needs attention today</option></select></label><label>What was observed?<textarea value={problem.note} onChange={event=>setProblem({...problem,note:event.target.value})} required/></label><button className="primary-button" type="submit">Save problem record</button></form>}</section></div>;
+ {view==='harvest'&&<form className="modal-form connected-record-form" onSubmit={submitHarvest}><div className="control-callout"><Leaf/><span><strong>Record what was actually picked.</strong><small>{draft.harvestPattern?.type==='repeat'?'This is a repeat-harvest crop; one harvest will not finish the planting.':'This crop is currently treated as a one-time harvest.'}</small></span></div><section className="known-record-context"><small>PREFILLED FROM THIS PLANT JOURNEY</small><strong>{plant.name}{plant.variety?` — ${plant.variety}`:''}</strong><span>{space?.name||'Unassigned Growing Space'} · {normalizeLifecycleStage(plant.stage)}</span>{packet&&<em>{packetDisplayName(packet,plant)}</em>}</section><label>Harvest date<input type="date" value={harvest.harvestedAt||''} onChange={event=>setHarvest({...harvest,harvestedAt:event.target.value})} required/></label><div className="two-field-row"><label>Amount<input type="number" min="0" step="0.1" value={harvest.amount} onChange={event=>setHarvest({...harvest,amount:Number(event.target.value)})}/></label><label>Unit<select value={harvest.unit} onChange={event=>setHarvest({...harvest,unit:event.target.value})}><option>count</option><option>ounces</option><option>pounds</option><option>handful</option><option>bunch</option><option>basket</option></select></label></div><label>Quality<select value={harvest.quality} onChange={event=>setHarvest({...harvest,quality:event.target.value})}><option>Excellent</option><option>Good</option><option>Okay</option><option>Poor</option></select></label><label>Use or destination<select value={harvest.useDestination||'Kitchen'} onChange={event=>setHarvest({...harvest,useDestination:event.target.value})}><option>Kitchen</option><option>Preserved</option><option>Shared</option><option>Donated</option><option>Compost</option><option>Other</option></select></label><label>Grow this again?<select value={harvest.growAgain} onChange={event=>setHarvest({...harvest,growAgain:event.target.value})}><option value="yes">Yes</option><option value="maybe">Maybe</option><option value="no">No</option></select></label><label className="photo-upload-button"><Camera/>{harvest.photo?'Replace harvest photo':'Add optional harvest photo'}<input type="file" accept="image/*" capture="environment" onChange={event=>addRecordPhoto(event.target.files?.[0],setHarvest)}/></label>{harvest.photo&&<div className="record-photo-preview"><img src={harvest.photo} alt="Harvest record"/><small>{photoCapabilityLabel(harvest)}</small></div>}<label>Harvest note<textarea value={harvest.note} onChange={event=>setHarvest({...harvest,note:event.target.value})}/></label><button className="primary-button" type="submit">Save harvest to Plant Journey</button></form>}
+ {view==='problem'&&<form className="modal-form connected-record-form" onSubmit={submitProblem}><div className="control-callout warning"><AlertTriangle/><span><strong>Log what you see, not a guess.</strong><small>Weather can provide context, but this form does not diagnose a photo. Recommended work stays connected through the Chore Board.</small></span></div><section className="known-record-context"><small>PREFILLED FROM THIS PLANT JOURNEY</small><strong>{plant.name}{plant.variety?` — ${plant.variety}`:''}</strong><span>{space?.name||'Unassigned Growing Space'} · {normalizeLifecycleStage(plant.stage)}</span></section><label>Observation date<input type="date" value={problem.observedAt||''} onChange={event=>setProblem({...problem,observedAt:event.target.value})} required/></label><label>Problem type<select value={problem.type} onChange={event=>setProblem({...problem,type:event.target.value})}><option>Pest spotted</option><option>Disease concern</option><option>Frost damage</option><option>Heat damage</option><option>Animal damage</option><option>Germination failure</option><option>Transplant failure</option><option>Plant died</option><option>General issue</option></select></label><label>What should happen next?<select value={problem.severity} onChange={event=>setProblem({...problem,severity:event.target.value})}><option>Keep watching</option><option>Needs attention soon</option><option>Needs attention today</option></select></label><label>What was observed?<textarea value={problem.note} onChange={event=>setProblem({...problem,note:event.target.value})} required/></label><label>Action already taken<textarea value={problem.actionTaken||''} onChange={event=>setProblem({...problem,actionTaken:event.target.value})} placeholder="Removed damaged leaves, rinsed pests, moved pot, or no action yet"/></label><label className="photo-upload-button"><Camera/>{problem.photo?'Replace issue photo':'Add optional issue photo'}<input type="file" accept="image/*" capture="environment" onChange={event=>addRecordPhoto(event.target.files?.[0],setProblem)}/></label>{problem.photo&&<div className="record-photo-preview"><img src={problem.photo} alt="Garden issue record"/><small>{photoCapabilityLabel(problem)}</small></div>}<button className="primary-button" type="submit">Save issue record</button></form>}</section></div>;
 }
diff --git a/src/GardenMemory.jsx b/src/GardenMemory.jsx
index e7cf8e2fcd33c68cbb5b1f2b359df2accb76a51f..2b7538233199b505fa9d61a3de8965159e6609d4 100644
--- a/src/GardenMemory.jsx
+++ b/src/GardenMemory.jsx
@@ -1,7 +1,15 @@
-import React, { useMemo, useState } from 'react';
-import { ArrowLeft, CheckCircle2, Leaf, Sprout, TriangleAlert } from 'lucide-react';
-export default function GardenMemory({ garden, navigate, resolveProblem }) {
- const [tab, setTab] = useState('overview'); const harvests = garden.harvests || []; const problems = garden.problems || []; const active = problems.filter((x) => x.status !== 'resolved'); const resolved = problems.filter((x) => x.status === 'resolved');
- const totals = useMemo(() => { const map = new Map(); harvests.forEach((h) => { const row = map.get(h.name) || { name: h.name, count: 0, amounts: {}, growAgain: h.growAgain }; row.count += 1; row.amounts[h.unit] = (row.amounts[h.unit] || 0) + (Number(h.amount) || 0); if (h.growAgain) row.growAgain = h.growAgain; map.set(h.name, row); }); return Array.from(map.values()).sort((a, b) => b.count - a.count); }, [harvests]);
- return <main className="screen memory-screen"><section className="dark-header memory-header"><button className="back-button" onClick={() => navigate('back')} aria-label="Go back"><ArrowLeft /></button><Sprout /><span>THE GARDEN REMEMBERS</span><h1>Garden Memory</h1><p>Harvests, problems, favorites, and what to change next season.</p></section><section className="memory-body screen-pad"><div className="memory-tabs">{['overview', 'harvests', 'problems', 'review'].map((item) => <button key={item} className={tab === item ? 'active' : ''} onClick={() => setTab(item)}>{item}</button>)}</div>{tab === 'overview' && <div className="memory-summary-grid"><article><strong>{harvests.length}</strong><span>Harvest records</span></article><article><strong>{totals.length}</strong><span>Crops harvested</span></article><article><strong>{active.length}</strong><span>Active issues</span></article><article><strong>{resolved.length}</strong><span>Resolved issues</span></article></div>}{tab === 'harvests' && <section className="memory-list">{totals.length ? totals.map((row) => <article key={row.name}><Leaf /><span><strong>{row.name}</strong><small>{row.count} harvest{row.count === 1 ? '' : 's'} · {Object.entries(row.amounts).map(([unit, amount]) => `${amount} ${unit}`).join(', ')}</small><small>{row.growAgain || 'No grow-again rating yet'}</small></span></article>) : <di