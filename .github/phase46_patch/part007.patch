v className="empty-memory"><Leaf /><span><strong>No harvests yet.</strong><small>Harvest records appear here automatically.</small></span></div>}</section>}{tab === 'problems' && <section className="memory-list">{active.map((problem) => <article key={problem.id}><TriangleAlert /><span><strong>{problem.type}</strong><small>{problem.severity} · {problem.note}</small></span><button onClick={() => resolveProblem(problem.id)}><CheckCircle2 /> Resolve</button></article>)}{!active.length && <div className="empty-memory"><CheckCircle2 /><span><strong>No active problems.</strong><small>Resolved issues remain part of the season history.</small></span></div>}</section>}{tab === 'review' && <section className="season-review"><span className="section-kicker">2026 SEASON REVIEW</span><h2>What the records say so far</h2><p>{totals[0] ? `${totals[0].name} has been harvested most often.` : 'Harvest some crops to begin the production review.'}</p><p>{active.length ? `${active.length} issue${active.length === 1 ? '' : 's'} still need attention.` : 'No unresolved garden problems are currently recorded.'}</p><p>{totals.filter((x) => String(x.growAgain).toLowerCase().includes('skip')).length ? 'At least one crop is marked to skip next season.' : 'No crops are currently marked to skip next season.'}</p></section>}</section></main>;
+import React,{useMemo,useState}from'react';
+import{ArrowLeft,Camera,CheckCircle2,Leaf,Sprout,TriangleAlert}from'lucide-react';
+import{formatDateTime}from'./data.js';
+import{photoCapabilityLabel}from'./connectedIntelligence.js';
+
+export default function GardenMemory({garden,navigate,resolveProblem}){
+ const[tab,setTab]=useState('overview'),harvests=(garden.harvests||[]).filter(row=>!row.deletedAt),problems=(garden.problems||[]).filter(row=>!row.deletedAt),active=problems.filter(row=>row.status!=='resolved'),resolved=problems.filter(row=>row.status==='resolved');
+ const totals=useMemo(()=>{const map=new Map();harvests.forEach(harvest=>{const key=harvest.plantId||`${harvest.name}|${harvest.variety||''}`,row=map.get(key)||{key,name:harvest.name||harvest.cropName||'Harvest',variety:harvest.variety||'',spaceName:harvest.spaceName||'',count:0,amounts:{},growAgain:harvest.growAgain};row.count+=1;row.amounts[harvest.unit]=(row.amounts[harvest.unit]||0)+(Number(harvest.amount)||0);if(harvest.growAgain)row.growAgain=harvest.growAgain;map.set(key,row)});return[...map.values()].sort((a,b)=>b.count-a.count)},[harvests]);
+ return <main className="screen memory-screen"><section className="dark-header memory-header"><button className="back-button" onClick={()=>navigate('back')} aria-label="Go back"><ArrowLeft/></button><Sprout/><span>THE GARDEN REMEMBERS</span><h1>Garden Memory</h1><p>Harvests, issues, photos, completed actions, and what to change next season.</p></section><section className="memory-body screen-pad"><div className="memory-tabs">{['overview','harvests','problems','review'].map(item=><button key={item} className={tab===item?'active':''} onClick={()=>setTab(item)}>{item}</button>)}</div>
+  {tab==='overview'&&<div className="memory-summary-grid"><article><strong>{harvests.length}</strong><span>Harvest records</span></article><article><strong>{totals.length}</strong><span>Plantings harvested</span></article><article><strong>{active.length}</strong><span>Active issues</span></article><article><strong>{resolved.length}</strong><span>Resolved issues</span></article></div>}
+  {tab==='harvests'&&<section className="memory-list connected-memory-list">{totals.length&&<div className="memory-totals">{totals.map(row=><article key={row.key}><Leaf/><span><strong>{row.name}{row.variety?` — ${row.variety}`:''}</strong><small>{row.count} harvest{row.count===1?'':'s'} · {Object.entries(row.amounts).map(([unit,amount])=>`${amount} ${unit}`).join(', ')}</small><small>{[row.spaceName,row.growAgain&&`Grow again: ${row.growAgain}`].filter(Boolean).join(' · ')}</small></span></article>)}</div>}<h2>Individual harvest records</h2>{harvests.length?harvests.map(harvest=><article key={harvest.id} className="memory-record-card">{harvest.photo?<img src={harvest.photo} alt={`${harvest.name||'Garden'} harvest`}/>:<Camera/>}<span><strong>{harvest.name}{harvest.variety?` — ${harvest.variety}`:''}</strong><small>{harvest.amount} {harvest.unit} · {harvest.quality||'Quality not recorded'} · {harvest.spaceName||'Growing Space not recorded'}</small><small>{formatDateTime(harvest.harvestedAt||harvest.at)}{harvest.useDestination?` · ${harvest.useDestination}`:''}</small>{harvest.photo&&<em>{photoCapabilityLabel(harvest)}</em>}{harvest.note&&<p>{harvest.note}</p>}</span></article>):<div className="empty-memory"><Leaf/><span><strong>No harvests yet.</strong><small>Harvest records appear here automatically from the Plant Journey.</small></span></div>}</section>}
+  {tab==='problems'&&<section className="memory-list connected-memory-list">{active.map(problem=><article key={problem.id} className="memory-record-card">{problem.photo?<img src={problem.photo} alt={`${problem.plantName||'Plant'} issue record`}/>:<TriangleAlert/>}<span><strong>{problem.type} · {problem.plantName||'Plant not recorded'}</strong><small>{problem.severity} · {problem.spaceName||'Growing Space not recorded'} · {formatDateTime(problem.observedAt||problem.at)}</small>{problem.note&&<p>{problem.note}</p>}{problem.actionTaken&&<small><b>Action taken:</b> {problem.actionTaken}</small>}{problem.photo&&<em>{photoCapabilityLabel(problem)}</em>}</span><button onClick={()=>resolveProblem(problem.id)}><CheckCircle2/> Resolve</button></article>)}{!active.length&&<div className="empty-memory"><CheckCircle2/><span><strong>No active problems.</strong><small>Resolved issues remain part of the season and Plant Journey history.</small></span></div>}{resolved.length>0&&<details className="resolved-problem-history"><summary>{resolved.length} resolved issue{resolved.length===1?'':'s'}</summary>{resolved.map(problem=><p key={problem.id}><strong>{problem.type}</strong> · {problem.plantName||'Plant'} · resolved {formatDateTime(problem.resolvedAt||problem.updatedAt||problem.at)}</p>)}</details>}</section>}
+  {tab==='review'&&<section className="season-review"><span className="section-kicker">2026 SEASON REVIEW</span><h2>What the connected records say so far</h2><p>{totals[0]?`${totals[0].name}${totals[0].variety?` — ${totals[0].variety}`:''} has been harvested most often.`:'Harvest some crops to begin the production review.'}</p><p>{active.length?`${active.length} issue${active.length===1?'':'s'} still need attention.`:'No unresolved garden problems are currently recorded.'}</p><p>{harvests.filter(row=>String(row.growAgain).toLowerCase()==='no').length?'At least one harvested planting is marked not to grow again.':'No harvested planting is currently marked to skip next season.'}</p></section>}
+ </section></main>;
 }
diff --git a/src/GardenShoppingList.jsx b/src/GardenShoppingList.jsx
index 63190382ec3a9e046c070e17064f5d64cc75f08f..f3f144659972d5564665fc94d3860c7638d44a71 100644
--- a/src/GardenShoppingList.jsx
+++ b/src/GardenShoppingList.jsx
@@ -13,11 +13,11 @@ function ItemEditor({item,garden,onSave,onCancel}){
 
 function ShoppingItemCard({item,garden,onUpdate,onRemove,onEdit,onSeedInventory,onAddPlant,onOpenSource}){
  const space=garden.spaces?.find(row=>row.id===item.intendedSpaceId),isSeed=item.category==='Seeds',isPlant=item.category==='Starter Plant';
- return <article className={`garden-shopping-item ${item.purchased?'is-purchased':''}`}><div className="shopping-item-icon" aria-hidden="true">{item.purchased?<PackageCheck/>:<ShoppingBasket/>}</div><div className="shopping-item-copy"><span><small>{item.category}</small>{item.purchased&&<em>PURCHASED</em>}</span><h2>{item.name}</h2><p>{[item.variety,item.brand].filter(Boolean).join(' · ')||item.reason||'Garden purchase'}</p><div className="shopping-item-meta"><span>Qty {item.quantity||1}</span>{space&&<span>{space.name}</span>}{item.desiredWindow&&<span>{item.desiredWindow}</span>}</div>{item.note&&<small>{item.note}</small>}</div><div className="shopping-item-actions">{item.purchased?<button onClick={()=>onUpdate(item,{purchased:false,purchasedAt:''})}><Undo2/>Undo purchased</button>:<button className="shopping-purchase-button" onClick={()=>onUpdate(item,{purchased:true,purchasedAt:new Date().toISOString()})}><Check/>Mark purchased</button>}<button onClick={()=>onEdit(item)}><Edit3/>Edit</button>{item.sourceRecommendation&&<button onClick={()=>onOpenSource(item)}>View recommendation</button>}{item.purchased&&isSeed&&!item.linkedSeedInventoryId&&<button className="shopping-handoff-button" onClick={()=>onSeedInventory(item)}><Sprout/>Add to Seed Inventory</button>}{item.purchased&&isPlant&&!item.linkedPlantId&&<button className="shopping-handoff-button" onClick={()=>onAddPlant(item)}><Leaf/>Add Plant</button>}<button className="shopping-remove-button" onClick={()=>onRemove(item)}><Trash2/>Remove</button></div></article>;
+ return <article className={`garden-shopping-item ${item.purchased?'is-purchased':''}`}><div className="shopping-item-icon" aria-hidden="true">{item.purchased?<PackageCheck/>:<ShoppingBasket/>}</div><div className="shopping-item-copy"><span><small>{item.category}</small>{item.purchased&&<em>PURCHASED</em>}{item.alreadyOwned&&<em>ALREADY OWNED</em>}</span><h2>{item.name}</h2><p>{[item.variety,item.brand].filter(Boolean).join(' · ')||item.reason||'Garden purchase'}</p><div className="shopping-item-meta"><span>Qty {item.quantity||1}</span>{space&&<span>{space.name}</span>}{item.desiredWindow&&<span>{item.desiredWindow}</span>}</div>{item.note&&<small>{item.note}</small>}</div><div className="shopping-item-actions">{!item.purchased&&!item.alreadyOwned&&<button onClick={()=>onUpdate(item,{alreadyOwned:true,ownedRecordId:item.linkedSeedInventoryId||item.linkedPlantId||''})}><Check/>Mark already owned</button>}{item.alreadyOwned&&<button onClick={()=>onUpdate(item,{alreadyOwned:false,ownedRecordId:''})}><Undo2/>Undo already owned</button>}{item.purchased?<button onClick={()=>onUpdate(item,{purchased:false,purchasedAt:''})}><Undo2/>Undo purchased</button>:<button className="shopping-purchase-button" onClick={()=>onUpdate(item,{purchased:true,purchasedAt:new Date().toISOString()})}><Check/>Mark purchased</button>}<button onClick={()=>onEdit(item)}><Edit3/>Edit</button>{item.sourceRecommendation&&<button onClick={()=>onOpenSource(item)}>View recommendation</button>}{item.purchased&&isSeed&&!item.linkedSeedInventoryId&&<button className="shopping-handoff-button" onClick={()=>onSeedInventory(item)}><Sprout/>Add to Seed Inventory</button>}{item.purchased&&isPlant&&!item.linkedPlantId&&<button className="shopping-handoff-button" onClick={()=>onAddPlant(item)}><Leaf/>Add Plant</button>}<button className="shopping-remove-button" onClick={()=>onRemove(item)}><Trash2/>Remove</button></div></article>;
 }
 
 export default function GardenShoppingList({garden,navigate,onAddItem,onUpdateItem,onRemoveItem,onClearPurchased,onBeginSeedInventory,onBeginPlant}){
- const[filter,setFilter]=useState('Needed'),[editing,setEditing]=useState(null),[adding,setAdding]=useState(false),items=useMemo(()=>gardenShoppingItems(garden),[garden]),needed=items.filter(item=>!item.purchased),purchased=items.filter(item=>item.purchased),visible=filter==='Purchased'?purchased:needed;
+ const[filter,setFilter]=useState('Needed'),[editing,setEditing]=useState(null),[adding,setAdding]=useState(false),items=useMemo(()=>gardenShoppingItems(garden),[garden]),needed=items.filter(item=>!item.purchased&&!item.alreadyOwned),purchased=items.filter(item=>item.purchased),visible=filter==='Purchased'?purchased:needed;
  const saveNew=data=>{const result=onAddItem(data);if(result?.status==='added')setAdding(false)};
  const saveEdit=data=>{onUpdateItem(editing,data);setEditing(null)};
  const openSource=item=>{sessionStorage.setItem('planting-desk-destination',item.desiredWindow&&item.desiredWindow>new Date().toISOString().