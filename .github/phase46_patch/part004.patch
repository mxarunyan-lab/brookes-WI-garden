me="modal-close" onClick={close}><X/></button>{modal.type==='addPlant'?<AddPlant {...props}/>:modal.type==='addSpace'?<AddSpace {...props}/>:<SoilCheck {...props}/>}</section></div>}
diff --git a/src/GardenCenter.jsx b/src/GardenCenter.jsx
index c5fc30fbc92d56c2a3b1d74380c8760c32cf5fe7..dfd9e0621c3782d0e62ffa72e8d44a7386a1b672 100644
--- a/src/GardenCenter.jsx
+++ b/src/GardenCenter.jsx
@@ -1,5 +1,5 @@
 import React from'react';
-import{BookOpen,CalendarDays,ChevronRight,ClipboardList,PackageSearch,ShoppingBasket,Sprout,Warehouse}from'lucide-react';
+import{BookOpen,CalendarDays,CalendarRange,ChevronRight,ClipboardList,PackageSearch,ShoppingBasket,Sprout,Warehouse}from'lucide-react';
 import{activeShoppingCount}from'./gardenShopping.js';
 import{SecondaryHero}from'./SecondaryUI.jsx';
 import{buildTaskBoard}from'./taskBoard.js';
@@ -7,7 +7,7 @@ import{buildTaskBoard}from'./taskBoard.js';
 function DepartmentTile({icon:Icon,title,status,onClick,tone='cream',featured=false}){return <button type="button" className={`garden-center-tile tone-${tone}${featured?' is-featured':''}`} onClick={onClick} aria-label={`${title}. ${status}`}><span className="garden-center-tile-icon" aria-hidden="true"><Icon/></span><span className="garden-center-tile-copy"><strong>{title}</strong><small>{status}</small></span><ChevronRight aria-hidden="true"/></button>}
 
 export default function GardenCenter({garden,navigate,tasks=[],dailyDone=[]}){
- const board=buildTaskBoard({tasks,completions:dailyDone}),due=board.counts.dueToday||0,packets=(garden.seedPackets||[]).filter(item=>!item.deletedAt).length,trays=(garden.trays||[]).filter(item=>!item.deletedAt).length,problems=(garden.problems||[]).filter(item=>item.status!=='resolved'&&!item.deletedAt).length,planned=(garden.yearPlan?.crops||[]).filter(item=>item.status==='definitely'||item.status==='maybe').length,shopping=activeShoppingCount(garden);
+ const board=buildTaskBoard({tasks,completions:dailyDone}),due=board.counts.dueToday||0,packets=(garden.seedPackets||[]).filter(item=>!item.deletedAt).length,trays=(garden.trays||[]).filter(item=>!item.deletedAt).length,problems=(garden.problems||[]).filter(item=>item.status!=='resolved'&&!item.deletedAt).length,planned=(garden.yearPlan?.crops||[]).filter(item=>item.status==='definitely'||item.status==='maybe').length,shopping=activeShoppingCount(garden),trip=(garden.vacationPlans||[]).find(plan=>!plan.deletedAt&&plan.status==='active');
  const openNext=()=>{sessionStorage.setItem('planting-desk-destination','next:supplies');navigate('plan-plant')};
  return <main className="screen secondary-screen garden-center-screen"><SecondaryHero icon={Sprout} eyebrow="ARCHIE & BROOKE’S GARDEN STORE" title="Runyan Garden Center" description="Planning, seeds, care, records, and the next useful garden move." className="garden-center-hero"/><section className="screen-pad secondary-screen-content garden-center-content"><div className="garden-center-compact-grid">
   <DepartmentTile icon={ClipboardList} title="Care Desk" status={`${due} due today`} tone="gold" onClick={()=>navigate('chores')}/>
@@ -15,6 +15,7 @@ export default function GardenCenter({garden,navigate,tasks=[],dailyDone=[]}){
   <DepartmentTile icon={ShoppingBasket} title="Garden Shopping List" status={`${shopping} item${shopping===1?'':'s'} needed`} tone="gold" onClick={()=>navigate('shopping-list')}/>
   <DepartmentTile icon={PackageSearch} title="Seed Department" status={`${packets} packet${packets===1?'':'s'}`} tone="green" onClick={()=>navigate('seed-tools')}/>
   <DepartmentTile icon={Warehouse} title="Indoor Growing" status={`${trays} active tray${trays===1?'':'s'}`} tone="blue" onClick={()=>navigate('indoor')}/>
+  <DepartmentTile icon={CalendarRange} title="Vacation Mode" status={trip?`${trip.duration} day trip plan active`:'Build a garden-specific trip plan'} tone="blue" onClick={()=>navigate('vacation')}/>
   <DepartmentTile icon={BookOpen} title="Records Counter" status={`${problems} active issue${problems===1?'':'s'}`} tone="cream" onClick={()=>navigate('memory')}/>
   <DepartmentTile icon={CalendarDays} title="Next Season" status={`${planned} draft crop${planned===1?'':'s'}`} tone="green" featured onClick={openNext}/>
  </div></section></main>;
diff --git a/src/GardenControls.jsx b/src/GardenControls.jsx
index 276cfe6937bf3ad6629de1be8d9991703682d63e..61cfc7f8a98f9d882272fd6ff2fc0f038525c7c9 100644
--- a/src/GardenControls.jsx
+++ b/src/GardenControls.jsx
@@ -3,27 +3,29 @@ import{AlertTriangle,Archive,Camera,CheckCircle2,ChevronDown,Clock3,Droplets,His
 import{buildPlantJourneyEvents,createStageHistoryEntry,normalizeLifecycleStage,normalizeStageHistory,PLANT_LIFECYCLE_STAGES}from'./gardenIntelligence.js';
 import{daysSincePlanting,hardeningWeatherAdvice,nextLifecycleStage,normalizeHardeningPlan,normalizePlantSeedToHarvest,packetDisplayName,packetForPlant}from'./seedToHarvest.js';
 import{formatDateTime}from'./data.js';
+import{photoCapabilityLabel,prefillHarvest,prefillIssue}from'./connectedIntelligence.js';
 
 const fileToDataUrl=file=>new Promise((resolve,reject)=>{const reader=new FileReader();reader.onload=()=>resolve(String(reader.result));reader.onerror=reject;reader.readAsDataURL(file)});
 const pretty=value=>value?String(value).replace('T',' ').slice(0,10):'Not recorded';
 function JourneyAccordion({title,subtitle,icon:Icon,children,open=false}){return <details className="journey-accordion" open={open}><summary><Icon/><span><strong>{title}</strong><small>{subtitle}</small></span><ChevronDown/></summary><div className="journey-accordion-body">{children}</div></details>}
 
 export default function GardenControls({modal,garden,weather,close,savePlant,archivePlant,deletePlant,saveHarvest,saveProblem,onHardeningAction,startHardening}){
- const saved=garden.plants.find(item=>item.id===modal.plant?.id)||modal.plant;if(!saved)return null;const plant=normalizePlantSeedToHarvest(saved,garden),[view,setView]=useState(modal.view||'plant'),[draft,setDraft]=useState({...plant,stage:normalizeLifecycleStage(plant.stage),stageHistory:normalizeStageHistory(plant,garden.profile?.gardenerName)}),[stageNote,setStageNote]=useState(''),[stagePhoto,setStagePhoto]=useState(''),[harvest,setHarvest]=useState({amount:1,unit:'count',quality:'Good',note:'',growAgain:'yes'}),[problem,setProblem]=useState({type:'Pest spotted',severity:'Keep watching',note:''}),packet=packetForPlant(garden,draft),space=garden.spaces.find(item=>item.id===draft.spaceId),journey=useMemo(()=>buildPlantJourneyEvents({plant:draft,activity:garden.activity||[],harvests:garden.harvests||[],problems:garden.problems||[]}),[draft,garden.activity,garden.harvests,garden.problems]),stageHistory=normalizeStageHistory(draft,garden.profile?.gardenerName),hardeningRaw=(garden.hardeningPlans||[]).filter(plan=>plan.plantId===plant.id&&!plan.deletedAt).sort((a,b)=>String(b.updatedAt||b.createdAt).localeCompare(String(a.updatedAt||a.createdAt)))[0],hardening=hardeningRaw?normalizeHardeningPlan(hardeningRaw,plant):null,weatherAdvice=hardeningWeatherAdvice(weather||{}),days=daysSincePlanting(draft),nextStage=nextLifecycleStage(draft),watering=(garden.activity||[]).filter(entry=>entry.plantId===plant.id&&['watered','soil'].includes(entry.type)).slice(0,12),photos=stageHistory.flatMap(entry=>(entry.photos||[]).map(photo=>({photo,stage:entry.stage,date:entry.enteredAt}))),completed=journey.filter(entry=>entry.completed).slice(0,12);
+ const saved=garden.plants.find(item=>item.id===modal.plant?.id)||modal.plant;if(!saved)return null;const plant=normalizePlantSeedToHarvest(saved,garden),[view,setView]=useState(modal.view||'plant'),[draft,setDraft]=useState({...plant,stage:normalizeLifecycleStage(plant.stage),stageHistory:normalizeStageHistory(plant,garden.profile?.gardenerName)}),[stageNote,setStageNote]=useState(''),[stagePhoto,setStagePhoto]=useState(''),[harvest,setHarvest]=useState(()=>{const value=prefillHarvest(garden,plant.id);return{...value,harvestedAt:String(value.harvestedAt||new Date().toISOString()).slice(0,10)}}),[problem,setProblem]=useState(()=>{const value=prefillIssue(garden,plant.id);return{...value,observedAt:String(value.observedAt||new Date().toISOString()).slice(0,10)}}),packet=packetForPlant(garden,draft),space=garden.spaces.find(item=>item.id===draft.spaceId),journey=useMemo(()=>buildPlantJourneyEvents({plant:draft,activity:garden.activity||[],harvests:garden.harvests||[],problems:garden.problems||[]}),[draft,garden.activity,garden.harvests,garden.problems]),stageHistory=normalizeStageHistory(draft,garden.profile?.gardenerName),hardeningRaw=(garden.hardeningPlans||[]).filter(plan=>plan.plantId===plant.id&&!plan.deletedAt).sort((a,b)=>String(b.updatedAt||b.createdAt).localeCompare(String(a.updatedAt||a.createdAt)))[0],hardening=hardeningRaw?normalizeHardeningPlan(hardeningRaw,plant):null,weatherAdvice=hardeningWeatherAdvice(weather||{}),days=daysSincePlanting(draft),nextStage=nextLifecycleStage(draft),watering=(garden.activity||[]).filter(entry=>entry.plantId===plant.id&&['watered','soil'].includes(entry.type)).slice(0,12),photos=stageHistory.flatMap(entry=>(entry.photos||[]).map(photo=>({photo,stage:entry.stage,date:entry.enteredAt}))),completed=journey.filter(entry=>entry.completed).slice(0,12);
  const save=event=>{event.preventDefault();const previous=normalizeLifecycleStage(plant.stage),next=normalizeLifecycleStage(draft.stage);let history=normalizeStageHistory(draft,garden.profile?.gardenerName);if(next!==previous){const at=new Date().toISOString();history=history.map((entry,index,rows)=>index===rows.length-1&&!entry.completedAt?{...entry,completedAt:at}:entry);history=[...history,createStageHistoryEntry({stage:next,enteredAt:at,notes:stageNote,photos:stagePhoto?[stagePhoto]:[],actor:garden.profile?.gardenerName,source:'Plant Control Center',classification:'Confirmed'})]}savePlant(plant.id,{...draft,stage:next,stageHistory:history,stageUpdatedAt:next!==previous?new Date().toISOString():draft.stageUpdatedAt});close()};
  const addStagePhoto=async file=>{if(!file)return;if(file.size>1800000)return window.alert('Choose a photo smaller than about 1.8 MB.');setStagePhoto(await fileToDataUrl(file))};
  const confirmStage=(stage,note)=>{const at=new Date().toISOString(),history=normalizeStageHistory(draft,garden.profile?.gardenerName).map((entry,index,rows)=>index===rows.length-1&&!entry.completedAt?{...entry,completedAt:at}:entry);savePlant(plant.id,{...draft,stage,stageUpdatedAt:at,stageHistory:[...history,createStageHistoryEntry({stage,enteredAt:at,notes:note,actor:garden.profile?.gardenerName,source:'Plant Control Center',classification:'Confirmed'})]});close()};
- const submitHarvest=event=>{event.preventDefault();saveHarvest(plant.id,harvest);close()};
- const submitProblem=event=>{event.preventDefault();saveProblem(plant.id,problem);if(/failure|plant died/i.test(problem.type))confirmStage('Failed',problem.note||`${problem.type} recorded.`);else close()};
+ const addRecordPhoto=async(file,setter)=>{if(!file)return;if(file.size>1800000){window.alert('Choose a photo smaller than about 1.8 MB.');return}try{const photo=await fileToDataUrl(file);setter(current=>({...current,photo,photoAnalysisStatus:'stored-only'}))}catch(error){console.error('[Runyan Garden] record photo could not be loaded',error)}};
+ const submitHarvest=event=>{event.preventDefault();const savedRecord=saveHarvest(plant.id,{...harvest,harvestedAt:harvest.harvestedAt?new Date(`${harvest.harvestedAt}T12:00:00`).toISOString():new Date().toISOString()});if(savedRecord!==false)close()};
+ const submitProblem=event=>{event.preventDefault();const savedRecord=saveProblem(plant.id,{...problem,observedAt:problem.observedAt?new Date(`${problem.observedAt}T12:00:00`).toISOString():new Date().toISOString()});if(savedRecord!==false)close()};
  return <div className="overlay modal-overlay" onMouseDown={close}><section className="detail-modal control-modal plant-journey-modal" onMouseDown={event=>event.stopPropagation()}><button cla